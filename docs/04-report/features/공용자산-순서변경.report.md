# 공용자산 순서 변경(드래그) (Report)

**일자**: 2026-06-25 | **대상**: services/project(equipment-resource), apps/web(EquipmentResourcesPanel)

## 구현
- EquipmentResource에 `sortOrder` 추가(schema), list orderBy sortOrder, `reorder(orderedIds)` 서비스 + `PATCH /reorder`(ADMIN).
- 관리화면 **드래그 재정렬**(프로젝트 태스크 방식: 드래그핸들 ⠿, before/after), equipmentResourceApi.reorder.
- 관리화면 순서가 **예약 자원 리스트 순서로 반영**.

## 관련
[[공용자산예약]] 확장.
