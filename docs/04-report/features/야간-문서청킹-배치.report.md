# 야간 문서 청킹(추출) 배치 (Report)

**일자**: 2026-06-26 | **대상**: ot-brain(nas-extract.sh, poll_changes.py)

## 구현
- `/home/oceantech/nas-extract.sh`: flock + `timeout 7200 docker compose --profile extract run --rm -e MIN_DOC_SIZE=1000 ot-extractor`. 09:00 KST(EXIF 야간배치 20:00~08:00 종료 후, CIFS 경합 회피).
- poll_changes.py classify(): `~$`/`._` 정크 SKIPPED 처리.
- EXIF와 겹쳐도 단일리더 추출은 안전(워커 적음), CIFS soft마운트로 동결 방지.

## 관련
[[nas-extractor-size-gate-libreoffice]] [[exif-worker-cifs-tuning]] [[nas-incremental-sync-index]].
