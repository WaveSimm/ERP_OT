# 자원-기여도-진척률 완료 보고

> PDCA: do → check → report (2026-06-24)
> 상태: 배포 완료 (project-service 재빌드·재기동, DB 컬럼 적용)

## 1. 구현 결과 (설계 대비)
- ✅ `SegmentAssignment`에 `contributionWeight`(분담율)·`progressPercent`(자원 본인 진척) 추가 + 마이그레이션 백필
- ✅ 세그먼트 진척 = Σ(분담율×진척)/Σ분담율, 태스크 = 일수가중 평균 (derived)
- ✅ `updateAssignmentProgress` API + 본인/관리자 권한
- ✅ 수동 진척(isManualProgress) 경로 폐기, 그리드/간트 수동 진척 입력 제거
- ✅ TaskDrawer: 자원별 [내 투입]·[분담]·[진척 바], 세그먼트 derived + 분담율 합 경고
- ✅ 내 대시보드 작업목록: 내 투입/분담/내 진척률 컬럼, 진척 편집=배정별
- ✅ D3 개정: 본인 진척 100% 완료 시 **본인(자원) 작업일지** 필수
- ✅ 투입률 % 단일화(HOURS UI 제거), 라벨 "내 투입률"

## 2. 검증 (check)
- tsc(backend/web) 통과, ESLint 0 errors
- 라우트 등록 확인(401), DB 컬럼·백필 316행 확인
- 수동 점검: 슬라이더 100% 게이트, 슬라이더 되돌림, 구간날짜 표시 버그 수정
- 자체 평가 matchRate ≈ 95% (설계 항목 전수 구현, UX 미세조정 다수 반영)

## 3. 알려진 한계 / 후속
- 자원 본인 진척률 복구는 삭제 undo에 미포함(의도) — 별도
- legacy `resourceId` 잔존(점진 drop 예정), XOR 제약은 DB 레벨 존재
