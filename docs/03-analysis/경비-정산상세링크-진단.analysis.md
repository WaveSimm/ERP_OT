# 경비 정산상세 링크 진단 (Analysis, 코드변경 없음)

**일자**: 2026-06-26 | **대상**: expense-service, apps/web me/dashboard

## 사용자 보고
me/대시보드 경비 "최근 정산"의 정산 링크가 깨진 듯. 백엔드 미구현/이관누락 의심.

## 진단 결과 — 백엔드·데이터 정상
- expense-service Up 9일 healthy, /health 200. 정산 데이터 **9건** 존재.
- 라우트 `/expense/settlements/{id}` HTTP 200(페이지 로드), 백엔드 `GET /settlements/{id}` **200**(로그 확인). /expense layout 가드 없음. 링크 href 정상.
- 단, 같은 id가 200+401 혼재 → **간헐적 세션토큰 만료** 가능성.

## 결론
구현/이관 누락 아님. 정확한 증상(클릭 시 화면) 확인 필요 — 사용자에게 재현 요청함. 재로그인 권고.
