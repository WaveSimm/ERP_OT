# 시범 릴리즈 기간 버그 역추적 감사 (2026-07-02)

> 목적: 1차 리팩토링(PR#11~#69, "런타임 무변경" 표방)이 실제로 회귀를 만들지 않았는지,
> 그리고 리팩토링 활동 중 무엇이 실버그를 잡았는지를 시범 열흘(6/23~7/2)의 fix 이력으로 실증.
> 리팩토링 2차 웨이브 우선순위(기준 문서 부록 A-3)의 근거 자료.

## 1. fix 커밋별 근본원인 분류 (10건)

분류: (a) 시범 신규 기능 자체 버그 / (b) 리팩토링발 회귀 / (c) 리팩토링 이전 잠복 버그 / (d) 인프라·환경

| 해시 | 날짜 | 요약 | 분류 | 근거 |
|------|------|------|:----:|------|
| `cd462c5` | 6/25 | 프로덕션 빌드 가능화 (standalone/Suspense) | (d) | prod-mode 빌드 불능 해소. 런타임 회귀 아님 |
| `609dcf5` | 6/29 | 500 방지 — 프록시 폴백 docker명 | (d) | rewrites 목적지 빌드타임 localhost bake. audit userId 잠복버그(c) 동시 수정 |
| `787463e` | 6/29 | 프로젝트 목록 전체 로드 | (c) | list가 백엔드 기본 20건만 요청 — 마이그레이션(39건)으로 처음 노출된 잠복 한계 |
| `0ac5683` | 6/29 | 선택 팝업 이름 정렬 | (a) | 신규 폴더 UI 폴리시 |
| `d44306e` | 6/30 | rate-limit 사용자별 키 | (d) | 전 사용자가 프록시 단일 IP로 유입 → IP기준 한도 공유 초과 |
| `356f000` | 6/30 | 완료 태스크 '기한초과' 배지 오표시 | (c) | 원조 대시보드 구현(`422a5b6`, 리팩토링 이전)의 DONE 제외 누락 |
| `9e08947` | 7/1 | 가정의날 월경계 UTC 누수 | (a) | 버그 라인이 가정의날 신규 커밋(`fcf2358`)에서 도입 — blame 확인 |
| `fdec6ff` | 7/1 | 부분휴가 날 출퇴근 누락 | (a) | 캡스 자동연동 × 부분휴가 — 신규 기능 상호작용 버그 |
| `afcbd5f` | 7/1 | prod 빌드 devDep 누락 방지 | (d) | NODE_ENV=production npm install이 devDep 스킵 |
| `78907f5` | 7/2 | 이사(소관) 후보 임원 그룹 기준 | (a) | '임원' 그룹 부서 재편으로 기존 후보 로직이 비게 됨 |

분포: (a) 4~5건 · (c) 2건 · (d) 3~4건 · **(b) 리팩토링발 회귀 0건**

## 2. 리팩토링발 회귀 = 0건 (확정)

- 10개 fix 중 리팩토링 PR(#11~#69)이 도입한 버그 라인을 고친 것은 없음(blame 역추적).
- 파일 겹침도 없음: 리팩토링 본체(equipment repository/application 계층)를 건드린 fix 0건.
- **"리팩토링 런타임 무변경" 주장은 시범 열흘 버그 이력으로 반증되지 않음 → 방법론 실증 졸업.**

## 3. 리팩토링 중 실버그를 잡은 활동

| 커밋 | 버그 | 잡은 활동 |
|------|------|-----------|
| `98ebd22` (PR#64) | purchase-order 전체입고 시 존재하지 않는 enum `"RECEIVED"` → PrismaClientValidationError (`as any`에 가려짐) | **통합테스트**가 receive 경로 실행으로 발견 (타이핑=촉매, 실행=포착) |
| `9c3b7a3` (PR#38) | web 베이스라인 타입에러 3건 | CI 타입체크 게이트 |
| PR#12 일련 | settlements Page 타입에러·import 오류 | 타입체크/빌드 |
| PR#71·76·78·79·81·82 | 결재 IDOR·Next CVE·CSP 등 | 독립 보안 감사 (리팩토링 지표 아님) |

순수 지표 작업(any 개수 감소) 단독으로 제품 런타임 버그를 드러낸 기록은 없음.

## 4. 판정 — 2차 웨이브 배분에 대한 함의

버그를 잡은 것은 **코드를 실행시킨 활동**(통합테스트·타입/빌드 게이트·보안 리뷰·prod 배포)이었고,
시범기간 실버그는 신규 기능 결함·환경 문제로서 타입 지표로는 잡히지 않는 부류였다.
→ **2차 웨이브는 위험 주도(버그 다발 지점 테스트) 우선, 지표 주도(any tail)는 곁다리** —
기준 문서 부록 A-3의 우선순위(attendance 테스트·검색 골든 1순위, any tail 곁다리)를 그대로 확정한다.

*감사 방법: master..feat/trial-release-ui-improvements의 fix 커밋 전수 blame 역추적 + 리팩토링 머지 커밋 메시지 분석. 수행: Claude Code 백그라운드 에이전트.*
