# [Plan] 구매 · 재고 · 결재 (Procurement, Inventory & Approval)

> **Feature**: 구매-재고-결재
> **Phase**: Plan
> **Created**: 2026-04-09
> **Updated**: 2026-04-10
> **Status**: Plan v1.5
> **Author**: AI + Team

---

## 1. 개요 (Overview)

구매(해외/국내) → 입고 → 회계처리 → 재고관리 → 출고/납품의 전 과정을 관리하는 모듈. 크게 두 영역으로 구분된다:

- **구매/회계 영역**: 해외 발주, 수입원가정산, 문서 발행 등 — 재무팀 + 영업팀 전용
- **재고관리 영역**: 재고 현황, 입출고, 창고 이동 등 — 전 부서(전 사원) 사용

국내 구매(전 사원 가능)와 해외 구매(영업팀) 모두 재고 시스템으로 연동되며, 회계 처리는 재무팀/영업팀만 접근한다.

### 배경 (Background)

현재 업무 흐름:

**해외 구매 (영업팀 + 재무팀)**:
1. **발주**: 해외 제조사에 발주 → 인보이스 수령 → 송금 (EUR/GBP/USD 다통화)
2. **선적/통관**: 선적 → 통관(관세·운반비·수수료) → 입고
3. **수입원가정산**: 관세업체에서 수입면장(수입신고필증) 수령 → 재무팀이 수입면장 기반으로 수입원가정산 작성 (송금액 + 관세/통관/운송비 = 수입원가)

**국내 구매 (전 사원)**:
4. **일반 구매**: 업무에 필요한 제품/자재를 국내에서 구매 → 입고 등록

**재고관리 (전 부서 공용)**:
5. **입고**: 해외/국내 구매 입고 시 재고번호(E01394_1 형태) 부여
6. **이동/출고**: 구매/창고이동/판매 이력 추적
7. **납품**: 고객사에 납품 시 재고에서 출고 처리

### 엑셀 분석 결과

**발주_인보이스.xlsx** (48행):
- 헤더: 계약#, 고객사, 제작사, 품목, 통화, 발주금액, 발주일, 선적예정, 선적일, 통관, 입고, 입고위치, 발주담당자, invoice#, due date, OA#, 통관담당, 비고
- 통화 그룹: EUR, GBP, USD 별 섹션
- 색상 코딩: 선적예정, 통관, 입고 상태별 색 구분
- 하단: 통화별 발주금액 합계, 통장잔고, 추가필요금액 계산

**수입원가정산.xlsx** (136행, 양식 반복):
- 구조: 1건당 1양식 (45행 반복)
- 항목: 신고번호, 공급자, 신고일
- 수입원가 = (1)송금액(외화×환율) + (2)관세+통관+운송료(관세, 국외운반비, 국내운반비, 통관수수료, 창고보관료, 취급수수료/송금수수료)
- 모델/규격별 수량·단가·금액 명세
- 공급가액 = 수입원가 / 1.1 (VAT 역산)

**재고합본.xlsx** (23행):
- 헤더: 분류, 구분, 재고번호, 일자-No., 품목명[규격명], 수량, 공급사, 제조사, 최초창고, 이동창고, 납품처, 시리얼/로트No., 단가, 공급가액, 합계, 프로젝트명, 담당자명, 원가번호, 기타정보
- 분류: 미착품, 상품, 원재료, 전기상품, 전기원재료
- 구분: 구매, 창고(이동), 판매
- 재고번호 형식: `E{5자리}_{순번}` (예: E01394_1)
- 각 분류별 소계

---

## 2. 문제 정의 (Problem Statement)

| 문제 | 영향 |
|------|------|
| 발주 현황이 엑셀에 분산, 실시간 추적 불가 | 선적/입고 지연 감지 어려움, 색상 코딩 수작업 |
| 수입원가정산이 수동 양식 복사/입력 | 원가 계산 오류 위험, 검색/조회 불편 |
| 재고가 엑셀 합본으로 관리 | 재고번호 중복 위험, 이동이력 추적 어려움 |
| 장비 마스터 등록 없이 자유 텍스트 입력 | 동일 장비 다른 이름으로 중복 등록 |
| 통화별 잔고/필요금액 수동 계산 | 송금 계획 수립 지연 |
| 회계시스템 별도 입력 필요 | 상법 회계 시스템은 연동 불가 — 이중 입력은 불가피하며, 체크리스트로 누락 방지 |

---

## 3. 목표 및 성공 지표 (Goals & Success Metrics)

### 목표
- 발주 → 선적 → 통관 → 입고 전 과정을 단일 화면에서 추적
- 수입원가정산 자동 계산 (송금액 + 부대비용 → 원가 산출)
- 재고번호 체계적 자동 부여 (장비 마스터 기반)
- 장비 마스터 등록 → 중복 방지 → 시리얼 부여 → 재고번호 생성
- 구매/입고/이동/출고 이력 완전 추적

### 성공 지표

| 지표 | 목표값 | 측정 방법 |
|------|--------|-----------|
| 발주 현황 조회 | < 3초 | 전체 발주 목록 로딩 시간 |
| 재고번호 중복 | 0건 | DB unique 제약 |
| 원가정산 작성 | 기존 대비 50% 시간 단축 | 사용자 피드백 |
| 엑셀 대체율 | 100% | 3개 엑셀 파일 사용 중단 |

---

## 4. 핵심 도메인 개념

### 4.1 장비 마스터 (Product Master)

장비의 "종류"를 등록하는 카탈로그. 실제 개별 제품(시리얼)과 분리.

```
ProductMaster (장비 마스터)
├── id, name, modelName, manufacturer
├── category: 상품 | 원재료 | 미착품
├── defaultCurrency, referencePrice
└── specs (JSON): 규격 정보
```

**중복 방지**: `(manufacturer + modelName)` unique 제약

### 4.2 계약 (Contract)

사업 단위로 부여되는 계약. 하나의 계약 아래 여러 발주가 생성됨.

```
Contract (계약)
├── contractNumber: 계약번호
├── name: 계약명 (사업명)
├── customer: 고객사 (최종 납품 고객)
├── status: ACTIVE | COMPLETED | CANCELLED
├── startDate, endDate
├── notes
└── orders[]: 해외 발주 목록 (1:N)
```

### 4.3 발주 (Purchase Order - 해외)

하나의 계약에 속하는 개별 발주 건. 영업팀 담당자가 발주부터 입고까지 전 과정을 트래킹.

```
OverseasOrder (해외 발주)
├── contractId: 계약 FK
├── orderNumber: "#26-066" 형식 (연도-순번)
├── manufacturer: 제조사
├── currency: EUR | GBP | USD
├── items[]: 발주 품목 목록 (1:N → OverseasOrderItem)
├── status: 상태 FSM (아래 참조, 부분 입고 지원)
├── 날짜:
│   ├── orderDate: 발주일 (결재 승인 후 해외 발주한 날)
│   ├── estimatedProductionEnd: 제작 완료 예정일
│   ├── estimatedShipDate: 선적 예정일
│   ├── actualShipDate: 실제 선적일
│   ├── customsDate: 통관일
│   └── arrivalDate: 입고일
├── 제작 진행:
│   ├── productionProgress: 제작 진행율 (0~100%)
│   ├── productionNotes: 제작 현황 메모 (최신 상태 기록)
│   └── progressHistory[]: 진행율 변경 이력 (1:N → OrderProgressLog)
├── 위치: arrivalLocation
├── 담당: orderedBy (영업팀 담당자), customsHandler (통관 담당)
├── invoiceNo, dueDate, oaNumber
├── totalAmount: 발주 총액 (외화)
├── totalAmountKRW: 원화 환산 금액 (참고용)
└── notes
```

**OverseasOrderItem (발주 품목)**:
```
OverseasOrderItem
├── id
├── orderId: 발주 FK
├── productMasterId: 장비 마스터 FK
├── name: 품목명
├── spec: 규격/사양
├── quantity: 발주 수량
├── receivedQuantity: 입고 수량 (기본 0, 부분 입고 시 누적)
├── unitPrice: 단가 (외화)
├── amount: 금액 (quantity × unitPrice)
├── receiptStatus: PENDING | PARTIALLY_RECEIVED | FULLY_RECEIVED
└── notes
```

**상태 FSM**:
```
[DRAFT] → [PENDING_APPROVAL] → [APPROVED] → [ORDERED] → [IN_PRODUCTION] → [SHIPPED] → [CUSTOMS] → [PARTIALLY_RECEIVED] → [ARRIVED] → [CLOSED]
               │                                              │                                │
               └→ [REJECTED]                                  └→ 진행율 0~100% 트래킹          └→ 일부 품목만 입고 시
                   (수정 후 재상신)                                 100% 도달 시 선적 준비             전체 입고 완료 시 ARRIVED

부분 입고 규칙:
├── 품목별 입고 처리: OverseasOrderItem.receivedQuantity 누적
├── 일부 품목 입고 → 발주 상태: PARTIALLY_RECEIVED
├── 전체 품목 receivedQuantity == quantity → 발주 상태: ARRIVED
└── 입고 시마다 해당 품목의 InventoryItem 생성 (미착품 → 상품 자동 전환)

Phase 1 (결재 없음): DRAFT → ORDERED → IN_PRODUCTION → SHIPPED → CUSTOMS → [PARTIALLY_RECEIVED →] ARRIVED → CLOSED
Phase 2 (결재 연동): DRAFT → PENDING_APPROVAL → APPROVED → ORDERED → ...
```

**제작 진행 이력 (OrderProgressLog)**:
```
OrderProgressLog (진행 이력)
├── id
├── orderId: 발주 FK
├── progress: 진행율 (%)
├── note: 메모 (예: "PCB 조립 완료, 테스트 진행 중")
├── updatedBy: 기록자 (영업팀 담당자)
└── createdAt: 기록일시
```

> 영업팀 담당자가 제조사와 소통하며 진행율을 주기적으로 업데이트.
> 진행율 100% → 선적 준비 상태 → 선적일 입력 시 SHIPPED로 전환.

### 4.3.1 발주 현황 대시보드

영업팀 담당자가 자신의 발주 현황을 한눈에 파악하는 핵심 화면.

**담당자별 발주 현황**:
```
┌─────────────────────────────────────────────────────────┐
│ 내 발주 현황 (심윤송)                          [전체 보기] │
├─────────┬──────┬──────┬──────┬──────┬──────┬──────┬─────┤
│         │ 결재대기│ 발주완료│ 제작중 │ 선적  │ 통관  │ 입고  │합계  │
│ EUR     │  1   │  2   │  3   │  1   │  0   │  2   │  9  │
│ GBP     │  0   │  1   │  0   │  0   │  1   │  0   │  2  │
│ USD     │  0   │  0   │  1   │  0   │  0   │  1   │  2  │
├─────────┼──────┴──────┴──────┴──────┴──────┴──────┤─────┤
│ EUR합계 │ €123,456                                │     │
│ GBP합계 │ £45,000                                 │     │
│ USD합계 │ $67,890                                 │     │
└─────────┴─────────────────────────────────────────┴─────┘
```

**발주 트래킹 목록** (담당자 기본 필터):
- 상태별 색상 코딩 (엑셀 색상 코딩 대체)
- 제작 진행율 바 표시 (IN_PRODUCTION 상태)
- 선적예정일 경과 시 경고 표시
- 통관 지연 경고
- 정렬: 상태순 / 날짜순 / 제조사순

**통화별 합계 요약** (엑셀 하단 합계 대체):
```
통화별 발주 합계:
├── EUR: €123,456 (발주 9건)
├── GBP: £45,000 (발주 2건)
├── USD: $67,890 (발주 2건)
├── 통장잔고: (수동 입력 또는 메모)
└── 추가필요금액: 합계 - 잔고 (자동 계산)
```

### 4.4 국내 구매 — 지출결의서 (Expense Request)

부서에서 지출결의서를 올리고, 결재라인을 통해 승인 후 재무팀으로 이관. 재무팀이 재고관리 대상 여부를 판정하고, 재고 대상이면 입고 시점에 재고번호를 부여.

지출결의서 = **ApprovalDocument (template: EXPENSE)** + **ExpenseFollowUp (재무 후속 처리)**.
결재 프로세스(작성/상신/승인/반려)는 범용 결재 프레임워크(ApprovalDocument)가 처리하고,
ExpenseFollowUp은 결재 **승인 후** 재무팀 고유 업무(접수/재고판정/입고)만 담당한다.

```
ApprovalDocument (EXPENSE 양식)          ExpenseFollowUp (후속 처리 데이터)
├── 결재 프로세스 (상신~승인)             ├── approvalDocumentId: FK
├── 동적 필드: 건명, 목적, 공급사,        ├── financeInfo:
│   품목 내역 (품목명/수량/단가/금액)      │   ├── receivedBy: 재무팀 접수자
├── 결재선, 참조자, 참고부서               │   ├── receivedAt: 접수일
└── postApprovalAction:                  │   ├── isInventoryTarget: 재고관리 대상 여부
    FINANCE_FORWARD                      │   ├── inventoryDecisionBy: 판정자
        │                                │   ├── inventoryDecisionAt: 판정일
        └→ 승인 시 ExpenseFollowUp        │   └── inventoryDecisionNote: 판정 사유
           자동 생성 → 재무팀 대시보드     ├── arrivalInfo:
                                         │   ├── arrivalDate: 입고일
                                         │   ├── arrivalLocation: 입고 위치
                                         │   └── confirmedBy: 입고 확인자
                                         ├── inventoryItemId?: 재고 연결 FK
                                         └── notes
```

**후속 처리 FSM** (ExpenseFollowUp.status — 결재 승인 이후만):
```
[FINANCE_RECEIVED] → [INVENTORY_DECIDED] → [ARRIVED] → [COMPLETED]
                           │
                           └→ isInventoryTarget = false: 재고 미대상 → 바로 COMPLETED
                           └→ isInventoryTarget = true: 입고 대기 → ARRIVED에서 재고번호 부여
```

### 4.5 수입원가정산 (Import Cost Settlement)

```
ImportCostSettlement (수입원가정산)
├── declarationNo: 신고번호
├── supplier: 공급자
├── declarationDate: 신고일
├── remittances[]: 송금 내역 (일자, 외화금액, 환율, 원화금액, invoiceNo)
├── duties[]: 관세/통관/운송 비용 (명시적 원가)
│   ├── type: 관세 | 국외운반비 | 국내운반비 | 통관수수료 | 창고보관료 | 취급수수료
│   ├── amount, vat, notes
│   └── awbNo (항공화물운송장)
├── extras[]: 비명시 부대비용 (특정 품목에 직접 귀속)
│   ├── name: 비용명 (예: 클리어런스, 검역비, 기타)
│   ├── amount: 금액
│   ├── targetItemId: 귀속 품목 FK (items 중 하나 지정)
│   └── notes
├── totalExtraCost: 비명시 부대비용 합계
├── totalImportCost: 수입원가합계 (자동 계산 = 송금액 + duties + extras)
├── supplyAmount: 공급가액 (= 원가 / 1.1)
├── vat: 부가가치세
└── items[]: 모델/규격별 명세 (수량, 단가, 금액)
    └→ adjustedUnitPrice: 원래 단가 + 귀속된 부대비용 반영 후 단가
```

**비명시 부대비용 귀속 규칙**:
- 각 부대비용 항목마다 귀속할 품목을 1개 지정 (전체 배분 아님)
- 한 품목에 여러 부대비용이 귀속될 수 있음
- 예: 클리어런스 100만원 → 품목A, 검역비 50만원 → 품목A (모두 품목A에 귀속)

### 4.6 재고 (Inventory)

```
InventoryItem (재고)
├── inventoryNo: "E01394_1" (자동 생성)
├── productMasterId: 장비 마스터 FK
├── serialNumber: 실제 시리얼/로트 번호 (개별 자산만)
├── trackingMode: INDIVIDUAL | BULK
│   ├── INDIVIDUAL: 1재고번호 = 1개체 (시리얼 추적, 장비·자산류)
│   └── BULK: 1재고번호 = N수량 (동일 사양 소모품, 수량 관리)
├── quantity: 수량 (INDIVIDUAL=1 고정, BULK=가변)
├── category: 미착품 | 상품 | 원재료 | 전기상품 | 전기원재료
│   └→ 입고 시 미착품 → 상품으로 자동 전환 (수동 변경 가능, 예: 원재료)
├── currentLocation: 현재 위치 (창고명)
├── currentStatus: 재고 | 출고 | 수리중
├── unitPrice, supplyAmount, totalAmount    ← 초기 원가
├── totalAdditionalCost: 추가비용 누적 합계 (자동 갱신)
├── totalCostOfOwnership: TCO = totalAmount + totalAdditionalCost (자동 계산)
├── projectName, assigneeName
├── sourceType: OVERSEAS_ORDER | EXPENSE_REQUEST  ← 입고 출처
├── sourceId: 해외발주ID 또는 지출결의서ID
├── costSettlementId?: 원가정산 연결 (해외 구매 시)
└── costEvents[]: 추가비용 이력 (1:N → AssetCostEvent)

InventoryTransaction (재고 이력)
├── inventoryItemId
├── type: 구매(입고) | 창고(이동) | 판매(출고)
├── date, sequenceNo (일자-No.)
├── fromLocation, toLocation, deliveryTo
├── projectName, assigneeName, costNumber
└── notes
```

### 4.7 자산 추가비용 이력 (Asset Cost History)

입고 이후 발생하는 모든 추가비용을 재고 단위로 추적. AS, 업그레이드, 부품/모듈 추가 등.

```
AssetCostEvent (자산 추가비용)
├── id
├── inventoryItemId: 재고 FK
├── type: AS | UPGRADE | PARTS_ADDITION | MODULE_ADDITION | REPAIR | OTHER
├── title: 비용 항목명 (예: "센서 모듈 추가", "펌웨어 업그레이드")
├── description: 상세 내역
├── vendor: 수행 업체/공급사
├── cost: 비용 금액 (KRW)
├── currency: 원화 외 결제 시 (EUR/USD/GBP)
├── foreignAmount: 외화 금액
├── exchangeRate: 적용 환율
├── eventDate: 발생일
├── performedBy: 처리 담당자
├── attachments[]: 증빙 첨부 (Attachment 연결)
├── relatedOrderId?: 관련 발주 FK (해외 부품 발주 시)
├── notes
├── createdBy: 등록자
└── createdAt
```

**재고 원가 연동 규칙**:
- 각 재고 아이템은 **초기 원가** (수입원가정산 또는 구매가) + **누적 추가비용**을 가짐
- `totalCostOfOwnership = initialCost + SUM(AssetCostEvent.cost)` (자동 계산)
- 재고 상세 페이지에서 초기 원가 + 추가비용 이력 + TCO 한눈에 확인

**추가비용 유형**:

| 유형 | 설명 | 예시 |
|------|------|------|
| AS | 사후 서비스, 수리 | 보증 수리, 유상 수리 |
| UPGRADE | 하드웨어/소프트웨어 업그레이드 | 펌웨어 업데이트, 사양 변경 |
| PARTS_ADDITION | 부품 추가 장착 | 센서 모듈, 확장 카드 |
| MODULE_ADDITION | 모듈 추가 | 통신 모듈, 전원 모듈 |
| REPAIR | 외부 수리 (수리관리와 별개) | 외주 수리, 교정 |
| OTHER | 기타 비용 | 운송비, 설치비 |

### 4.8 범용 전자결재 프레임워크 (Approval Framework)

모든 결재 업무를 하나의 프레임워크로 처리. ecount ERP 19종 양식을 포괄하며, 양식 추가가 자유로운 구조.

#### 4.8.1 결재 양식 (ApprovalTemplate)

결재 문서의 "종류"를 정의. 관리자가 등록/수정.

```
ApprovalTemplate (결재 양식)
├── id
├── code: 양식 코드 (unique, 예: "EXPENSE", "PURCHASE_ORDER")
├── name: 양식명 (예: "지출결의서", "기안서_사전결재")
├── category: 양식 분류
│   └→ GENERAL | PROCUREMENT | ATTENDANCE | HR | WELFARE
├── description: 양식 설명
├── fields[]: 동적 필드 정의 (JSON)
│   └→ [{ key, label, type(text|number|date|select|textarea), required, options? }]
├── defaultApprovalLineRule: 기본 결재선 규칙
│   └→ DEPARTMENT_LINE: 부서 결재라인 사용 (기존 ApprovalLine)
│   └→ FIXED: 고정 결재자 지정 (예: 관리팀장 고정)
│   └→ AMOUNT_BASED: 금액 기준 (100만원 이상 대표이사 추가)
├── postApprovalAction: 승인 후 자동 동작 (선택)
│   └→ null: 없음 (기안서, 경위서 등)
│   └→ FINANCE_FORWARD: 재무팀 전달 (지출결의서)
│   └→ ORDER_CONFIRM: 발주 확정 (발주서)
│   └→ INVENTORY_RELEASE: 출고 처리 (출고신청서)
│   └→ LEAVE_APPROVE: 휴가 승인 처리 (향후 근태 마이그레이션)
│   └→ OT_APPROVE: OT 승인 처리 (향후 근태 마이그레이션)
├── relatedService: 연동 서비스 (후처리 HTTP 콜백 대상)
│   └→ null | "equipment-service" | "attendance-service" | "project-service"
├── isActive: 사용 여부
├── sortOrder: 정렬 순서
└── createdAt, updatedAt
```

#### 4.8.2 결재 문서 (ApprovalDocument)

실제 기안/상신되는 개별 결재 건.

```
ApprovalDocument (결재 문서)
├── id
├── documentNumber: 문서번호 (자동 채번, 예: "EXP-2026-0142")
├── templateId: 결재 양식 FK
├── title: 결재 제목
├── requestedBy: 기안자 (userId)
├── department: 기안 부서
├── approvalStepCount: 결재 단계 수 (1 | 2 | 3)
│   └→ 기안 시 사용자가 선택 (기본값: ApprovalLine에서 자동 감지)
│   └→ 1단계: 기안자 → 1차 결재자 → 완료
│   └→ 2단계: 기안자 → 1차 → 2차 결재자 → 완료
│   └→ 3단계: 기안자 → 1차 → 2차 → 3차 결재자 → 완료
├── content: 동적 필드 데이터 (JSON — template.fields에 대응)
├── amount: 결재 금액 (해당 시)
├── referenceType: 연결 대상 유형 (선택)
│   └→ null | OVERSEAS_ORDER | EXPENSE_REQUEST | INVENTORY_ITEM | LEAVE | OVERTIME
├── referenceId: 연결 대상 ID (선택)
├── status: 결재 상태 FSM (아래 참조)
├── attachments[]: 첨부파일 목록 (1:N → Attachment)
├── notes: 기안 비고
├── referenceDepts[]: 참고부서 목록 (departmentId[])
│   └→ 결재 완료 후 해당 부서에 문서 전달 (열람용)
├── referencePersons[]: 참고담당자 목록 (userId[])
│   └→ 결재 완료 후 개인에게 문서 전달 (열람용)
├── submittedAt: 상신 일시
├── completedAt: 최종 처리 일시
├── forwardedAt: 참고부서/담당자 전달 일시
├── createdAt, updatedAt
└── steps[]: 결재 단계 목록 (1:N → ApprovalStep, 개수 = approvalStepCount)
```

#### 4.8.3 결재 단계 (ApprovalStep)

각 문서의 N단 결재 진행 상태.

```
ApprovalStep (결재 단계)
├── id
├── documentId: 결재 문서 FK
├── stepOrder: 단계 순번 (1, 2, 3)
├── roleName: 직책명 스냅샷 (예: "팀장", "이사", "대표이사") — PDF 결재란 표시용
├── approverId: 결재자 (userId)
├── approverName: 결재자명 (스냅샷)
├── status: PENDING | APPROVED | REJECTED | SKIPPED
├── comment: 결재 의견
├── actedAt: 처리 일시
└── createdAt

> **결재 단계 생성 규칙**:
> - 문서 생성 시 `approvalStepCount`만큼 ApprovalStep 레코드 생성
> - 결재자는 기안자의 ApprovalLine에서 순서대로 매핑:
>   - 1단계: approverId만
>   - 2단계: approverId + secondApproverId
>   - 3단계: approverId + secondApproverId + thirdApproverId
> - 수동 변경 가능 (결재선 검색으로 결재자 교체)
```

#### 4.8.4 결재 상태 FSM

```
[DRAFT] → [AGREEMENT_PENDING] → [SUBMITTED] → [STEP_1_PENDING] → [STEP_2_PENDING] → [STEP_3_PENDING] → [APPROVED]
               │                     │                │                    │                    │
               └→ RETURNED           │                └── REJECTED ────────┴── REJECTED ────────┘
               (합의자 부동의)         │
                                     └→ 합의자 없으면 바로 SUBMITTED
                                     └→ 결재 단계 수는 approvalStepCount에 따라 1~3단 가변
                                        (선택한 단계 이후는 자동 스킵)

승인 완료 후:
[APPROVED] → ① postApprovalAction 자동 실행 + ② 참고부서/담당자 전달
    │
    ├─ ① postApprovalAction (비즈니스 후처리):
    │   ├→ FINANCE_FORWARD: 재무팀 대시보드에 표시
    │   ├→ ORDER_CONFIRM: 발주 상태 → ORDERED
    │   ├→ INVENTORY_RELEASE: 재고 출고 트랜잭션 생성
    │   ├→ LEAVE_APPROVE: WorkScheduleEntry upsert (향후)
    │   └→ OT_APPROVE: WorkScheduleEntry upsert (향후)
    │
    └─ ② 참고 전달 (referenceDepts[] + referencePersons[]):
        ├→ 참고부서 전체 구성원에게 알림 + 참고함에 문서 표시
        ├→ 참고담당자 개인에게 알림 + 참고함에 문서 표시
        └→ 참고 수신자는 열람만 가능 (결재 권한 없음)
```

#### 4.8.5 첨부파일 (Attachment)

```
Attachment (첨부파일)
├── id
├── referenceType: APPROVAL_DOCUMENT | SETTLEMENT | ORDER | INVENTORY
├── referenceId: 대상 문서 ID
├── fileName: 원본 파일명
├── storagePath: 저장 경로
├── fileSize: 파일 크기
├── mimeType: image/jpeg, application/pdf 등
├── isScanned: 스캔 촬영 여부
├── uploadedBy: 업로드자
└── createdAt
```

#### 4.8.6 초기 등록 양식 (ecount 분석 기반 18종)

| # | code | name | category | 결재선 규칙 | postApprovalAction |
|---|------|------|----------|-----------|-------------------|
| 1 | DRAFT_PRE | 기안서_사전결재(100만원 이상) | GENERAL | DEPARTMENT_LINE | null |
| 2 | DRAFT_POST | 기안서_사후결재(100만원 이상) | GENERAL | DEPARTMENT_LINE | null |
| 3 | PURCHASE_ORDER_SMALL | 발주서(100만원 미만) | PROCUREMENT | DEPARTMENT_LINE | ORDER_CONFIRM |
| 4 | EXPENSE | 지출결의서 | PROCUREMENT | DEPARTMENT_LINE | FINANCE_FORWARD |
| 5 | EXPENSE_AI | 지출결의서_AI 구독료 | PROCUREMENT | DEPARTMENT_LINE | FINANCE_FORWARD |
| 6 | BUSINESS_TRIP | 출장보고서 | GENERAL | DEPARTMENT_LINE | null |
| 7 | INVENTORY_RELEASE | 출고신청서 | PROCUREMENT | DEPARTMENT_LINE | INVENTORY_RELEASE |
| 8 | PURCHASE_ORDER | Purchase Order (해외) | PROCUREMENT | AMOUNT_BASED | ORDER_CONFIRM |
| 9 | LEAVE | 휴가신청서 | ATTENDANCE | DEPARTMENT_LINE | LEAVE_APPROVE |
| 10 | HOLIDAY_WORK | 휴일근로 신청서 | ATTENDANCE | FIXED(관리팀) | OT_APPROVE |
| 11 | FLEX_WORK | 유연(탄력)근무신청서 | ATTENDANCE | DEPARTMENT_LINE | null |
| 12 | CHILDCARE_SHORT | 육아기 근로시간 단축 | ATTENDANCE | DEPARTMENT_LINE | null |
| 13 | PARENTAL_LEAVE | 육아휴직 | ATTENDANCE | DEPARTMENT_LINE | null |
| 14 | FAMILY_CARE | 가족돌봄휴가(휴직) | ATTENDANCE | DEPARTMENT_LINE | null |
| 15 | HOUSING_FUND | 주택자금 신청서 | WELFARE | DEPARTMENT_LINE | null |
| 16 | INCIDENT_REPORT | 경위서 | HR | DEPARTMENT_LINE | null |
| 17 | APOLOGY | 시말서 | HR | DEPARTMENT_LINE | null |
| 18 | RESIGNATION | 사직서 | HR | DEPARTMENT_LINE | null |

#### 4.8.7 양식별 동적 필드 상세 (ecount 스크린샷 분석)

**기안서_사전결재** (DRAFT_PRE) — 구매/발주 목적:
```json
{
  "fields": [
    { "key": "projectRef", "label": "프로젝트", "type": "text", "placeholder": "#26- 계약번호_사업명" },
    { "key": "vendor", "label": "업체명", "type": "text", "required": true },
    { "key": "paymentTerms", "label": "결제조건", "type": "text", "required": true },
    { "key": "manager", "label": "담당자", "type": "text", "required": true },
    { "key": "orderDate", "label": "발주예정일", "type": "date", "required": true },
    { "key": "deliveryDeadline", "label": "납품기한", "type": "date", "required": true },
    { "key": "contactInfo", "label": "연락처", "type": "text", "required": true },
    { "key": "attachment", "label": "붙임", "type": "text", "placeholder": "수신 견적서 및 발주서 (파일첨부)" },
    { "key": "body", "label": "본문", "type": "richtext", "placeholder": "구매사유 및 필요내용을 자세히 기재" }
  ],
  "itemsTable": {
    "columns": ["내역", "단가", "수량", "소계", "부가세"],
    "autoSum": true,
    "vatRate": 0.1,
    "totalLabel": "합계 (VAT 포함)"
  },
  "footer": "본 기안은 자금 사용 승인과 외부 발주를 목적으로 합니다."
}
```

**지출결의서** (EXPENSE) — 국내 구매/지출:
```json
{
  "fields": [
    { "key": "projectRef", "label": "프로젝트", "type": "text", "placeholder": "#26-55 사업명" },
    { "key": "purpose", "label": "지출목적", "type": "text", "required": true },
    { "key": "totalAmount", "label": "합계금액", "type": "currency", "required": true, "autoCalculate": true }
  ],
  "itemsTable": {
    "columns": ["적요", "금액(부가세 포함)", "비고"],
    "autoSum": true,
    "totalLabel": "합계"
  },
  "footer": "위 금액을 청구하오니 결재 바랍니다."
}
```

**결재란 레이아웃** (PDF 출력 시):
```
기안서: [기안자] [결재] ← 1단 (또는 2단)
지출결의서: [기안자] [이사] [대표이사] ← 3단
양식별로 결재란 칸 수가 ApprovalStep 개수에 따라 동적 생성
```

#### 4.8.8 결재 작성 UI 기능 (ecount 분석)

ecount 화면에서 확인된 기능:

| 기능 | 설명 | 구현 대상 |
|------|------|----------|
| 양식 선택 드롭다운 | 상단에서 양식 변경 가능 | ✅ Phase 2 |
| 결재라인 검색 | My결재라인 + 부서별 결재선 템플릿 선택 | ✅ 기존 ApprovalLine 활용 |
| 결재자/참조자/합의자 | 결재자 외에 참조자(CC), 합의자 지정 | ✅ Phase 2 |
| 리치 텍스트 에디터 | 본문 WYSIWYG 편집 | ✅ Phase 2 |
| 첨부파일 | 다중 파일 첨부 | ✅ Phase 2 |
| 세부 내역 테이블 | 품목 행 추가/삭제, 자동 합계, VAT 자동 계산 | ✅ Phase 2 |
| 저장(결재올리기) | 임시저장(DRAFT) + 상신(SUBMITTED) | ✅ Phase 2 |
| 결재 단계 선택 | 1/2/3단계 결재 선택 | ✅ Phase 2 |
| 결재란 동적 렌더링 | 선택 단계 수에 맞춰 PDF 결재 칸 변동 | ✅ Phase 2 |
| 참고부서/담당자 | 결재 완료 후 부서/개인에게 전달 | ✅ Phase 2 |
| 양식 미리보기 | PDF 형태 미리보기 (결재란 포함) | ✅ Phase 6 (문서 발행) |
| M/S 일괄서명 | 다건 일괄 서명 | Phase 9 (고도화) |

#### 4.8.9 결재 문서 확장 필드

스크린샷에서 발견된 추가 필드 — ApprovalDocument에 반영:

```
ApprovalDocument (결재 문서) — 추가 필드
├── ccUsers[]: 참조자 목록 (userId[]) — 결재 진행 중 열람 가능 (의견 불요)
├── agreementUsers[]: 합의자 목록 (userId[]) — 결재 전 사전 합의 필요 (의견 필수)
├── richBody: 리치 텍스트 본문 (HTML)
├── itemsData: 세부 내역 테이블 (JSON[])
│   └→ [{ description, unitPrice, quantity, subtotal, vat }]
├── itemsTotal: 합계 금액 (자동 계산)
├── templateFooter: 양식 하단 문구 (양식에서 상속)
└── pdfGeneratedAt: PDF 생성 일시 (미리보기/출력 시)

> **참조자 vs 합의자 vs 참고부서/담당자 구분**:
> | 구분 | 시점 | 권한 | 용도 |
> |------|------|------|------|
> | **ccUsers** (참조자) | 결재 진행 중 | 열람만 가능 | 정보 공유 (FYI) |
> | **agreementUsers** (합의자) | 결재 시작 전 | 의견 필수 입력 (동의/부동의) | 타 부서 사전 합의 |
> | **referenceDepts/Persons** (참고부서/담당자) | 결재 완료 후 | 열람만 가능 | 후속 업무 전달 |
>
> **합의자 프로세스**: 상신 시 합의자에게 먼저 알림 → 전원 동의 후 결재 시작.
> 합의자 부동의 시 기안자에게 반환 (결재선 진입 전).
>
> **PDF 결재란 동적 렌더링**:
> - `approvalStepCount`에 따라 결재란 컬럼 수 변동
> - 1단계: | 기안 | 결재 |
> - 2단계: | 기안 | 결재 | 결재 |  (ecount 지출결의서 양식 참고)
> - 3단계: | 기안 | 결재 | 결재 | 결재 |  (ecount 기안서 양식 참고)
> - 각 컬럼에 roleName (직책), approverName (성명), actedAt (일자), 서명/도장 표시
> - 참고부서/담당자는 결재란 하단 별도 영역에 표시
```

#### 4.8.10 결재 UI

```
apps/web/src/app/approval/
├── layout.tsx              # 서브탭 레이아웃
├── page.tsx                # 결재 대기함 (내가 결재해야 할 문서)
├── sent/page.tsx           # 상신함 (내가 올린 문서)
├── cc/page.tsx             # 참조함 (내가 CC인 문서 + 참고 전달받은 문서)
├── completed/page.tsx      # 결재 완료함
├── new/page.tsx            # 새 결재 작성 (양식 선택 → 동적 폼 → 내역 테이블 → 상신)
├── [id]/page.tsx           # 결재 문서 상세 (승인/반려/의견 + PDF 미리보기)
└── admin/
    └── templates/page.tsx  # 양식 관리 (ADMIN)
```

**주요 화면**:
- **결재 대기함**: 내 결재 차례인 문서 목록 (배지 카운트 → AppLayout 헤더)
- **새 결재 작성**:
  - 양식 선택 드롭다운 → 동적 필드 폼 자동 생성
  - **결재 단계 선택**: 1단계 / 2단계 / 3단계 라디오 (기본값: ApprovalLine 기반 자동 감지)
  - 결재선: My결재라인 자동 로드 + 수동 변경 가능
    - 선택한 단계 수에 따라 결재자 입력 필드 동적 표시
  - 참조자(CC) / 합의자 검색 추가
  - **참고부서 선택**: 부서 검색 → 복수 선택 가능 (결재 완료 후 전달)
  - **참고담당자 선택**: 사용자 검색 → 복수 선택 가능 (결재 완료 후 전달)
  - 세부 내역 테이블 (행 추가/삭제, 단가×수량=소계, VAT 자동, 합계)
  - 리치 텍스트 에디터 (아래 §4.8.12 참조)
  - 첨부파일 (드래그앤드롭 + 카메라 스캔)
  - 임시저장(DRAFT) / 상신(SUBMITTED)
  - **PDF 미리보기**: 결재란이 선택한 단계 수에 맞춰 동적 렌더링
- **결재 상세**: 결재 단계 타임라인 + 승인/반려 버튼 + 의견 입력
- **상신함**: 내가 올린 문서의 진행 상태 추적
- **참조함**: 내가 참조자(CC)로 지정된 문서 + 참고 전달받은 문서 (탭 구분)

#### 4.8.11 근태 결재 마이그레이션 계획

현재 attendance-service 내부에 하드코딩된 휴가/OT 결재를 범용 프레임워크로 이관.

```
Phase A: 범용 프레임워크 완성 (해외구매-재고관리 Phase 2)
    └→ 지출결의서, 발주서, 기안서 등으로 검증

Phase B: 근태 결재 마이그레이션 (별도 Phase)
    └→ 휴가신청 → ApprovalDocument (template: LEAVE) 생성
    └→ OT신청 → ApprovalDocument (template: HOLIDAY_WORK) 생성
    └→ 승인 시 postApprovalAction → attendance-service 내부 API 호출
       └→ POST /internal/leave/{id}/approve
       └→ POST /internal/overtime/{id}/approve
    └→ 기존 LeaveService.approve(), OvertimeService.approve() 내부 로직은 유지
       (WorkScheduleEntry upsert 등)
    └→ 기존 leave.routes/overtime.routes의 approve/reject 엔드포인트는
       프레임워크 경유로 전환 (직접 호출 → deprecated)
    └→ 팀 근태 페이지(/me/team)의 승인 UI → 결재 대기함으로 통합
```

#### 4.8.12 리치 텍스트 에디터

결재 문서 본문 작성용 WYSIWYG 에디터. 요구사항 변경 시 업데이트 예정.

```
라이브러리: TipTap (ProseMirror 기반, MIT 라이선스)
패키지: @tiptap/react, @tiptap/starter-kit, @tiptap/extension-table, @tiptap/extension-image

기본 기능:
├── 서식: 볼드, 이탤릭, 밑줄, 취소선
├── 제목: H1 ~ H3
├── 목록: 순서/비순서, 체크리스트
├── 정렬: 좌/중/우/양쪽
├── 테이블: 행/열 추가·삭제, 셀 병합 (본문 내 표)
├── 이미지: 붙여넣기(클립보드) + 업로드 → 서버 저장 후 URL 삽입
├── 파일 링크: 첨부파일 참조 링크 삽입
└── 양식 템플릿: 양식별 기본 본문 자동 로드 (template.defaultBody)

저장 형식: HTML (richBody 필드)
PDF 변환: HTML → PDF 렌더링 시 에디터 스타일 유지
```

> **확장 가능**: 에디터 요구사항은 Phase 2 구현 시점에 재검토.
> 필요에 따라 멘션(@사용자), 코멘트, 수식 등 TipTap 확장 추가 가능.

### 4.9 스마트폰 스캔 + 이미지 최적화

```
스캔 흐름:
[1] 스마트폰 카메라 촬영 (HTML5 <input type="file" capture="environment">)
[2] 클라이언트: 미리보기 표시
[3] 서버 업로드
[4] 서버: 이미지 최적화 (sharp 라이브러리)
    └→ 문서 판독 가능 수준으로 리사이징 (장변 최대 2000px)
    └→ JPEG 품질 80% 압축
    └→ 원본 대비 약 70~90% 용량 절감
[5] 최적화된 이미지 저장 + 메타데이터 기록
```

### 4.10 재고 실사 (Physical Inventory Count)

현장에서 실제 재고를 확인하고 시스템 데이터와 대조하는 기능.

```
InventoryAudit (실사)
├── id
├── name: 실사명 (예: "2026년 상반기 정기 실사")
├── status: PLANNED → IN_PROGRESS → COMPLETED
├── plannedDate: 실사 예정일
├── startedAt, completedAt
├── createdBy: 실사 생성자
├── notes
└── items[]: 실사 항목 목록

InventoryAuditItem (실사 항목)
├── auditId: 실사 FK
├── inventoryItemId: 재고 FK
├── systemQuantity: 시스템 수량 (실사 시점 스냅샷)
├── systemLocation: 시스템 위치
├── actualQuantity: 실제 수량 (현장 입력)
├── actualLocation: 실제 위치 (현장 입력)
├── status: PENDING | MATCHED | MISMATCHED | MISSING
├── checkedBy: 확인자
├── checkedAt: 확인 일시
├── photo: 현장 사진 (Attachment 연결, 선택)
└── notes: 불일치 사유
```

**실사 흐름**:
```
[1] 실사 계획 생성 → 대상 재고 목록 자동 로드
[2] 현장 실사 (스마트폰)
    └→ 재고번호 검색 또는 목록에서 선택
    └→ 실제 수량/위치 입력 + 사진 촬영(선택)
    └→ 시스템 자동 비교 → MATCHED | MISMATCHED | MISSING 표시
[3] 실사 완료 → 차이 보고서 자동 생성
    └→ 불일치 항목 목록 + 사유
    └→ PDF/엑셀 출력 (회계감사 증빙용)
[4] 차이 조정 (승인 후 시스템 수량 보정)
```

### 4.11 회계감사 자료 (Audit Reports)

회계감사 시 필요한 자료를 기간/유형별로 출력.

```
출력 가능 보고서:
├── 구매 내역서: 기간별 해외/국내 구매 목록 (계약번호, 금액, 증빙 첨부 여부)
├── 수입원가정산 내역서: 기간별 원가정산 목록 (신고번호, 송금액, 관세, 총원가)
├── 재고 현황표: 기준일 시점 전체 재고 목록 (분류별, 금액 소계)
├── 재고 실사 보고서: 실사 결과 (시스템 vs 실제, 차이 내역)
├── 재고 입출고 명세서: 기간별 입고/출고/이동 트랜잭션 전체
└── 결재 이력: 기간별 결재 내역 + 증빙 첨부 목록

출력 형식: PDF (인쇄용) + 엑셀 (데이터 가공용)
```

### 4.12 재고번호 생성 규칙

1. 장비 마스터에서 품목 선택 (또는 신규 등록)
2. 시리얼 번호 입력
3. 시스템이 자동 생성: `E{5자리 시퀀스}_{품목내 순번}`
   - E = Equipment 접두사
   - 5자리: 전체 시퀀스 (DB auto-increment)
   - _N: 같은 발주 내 동일 품목의 순번

---

## 5. 역할 및 권한 (Roles)

| 역할 | 구매/회계 영역 | 재고 영역 |
|------|---------------|-----------|
| **전 사원** | 지출결의서 작성/상신 | 재고 조회, 입출고 등록, 창고 이동, 추가비용 등록 |
| **부서장** | 지출결의서 결재 (결재라인) | 전체 접근 |
| **영업팀** | 계약 등록, 해외 발주 생성/관리, 선적·통관·입고 추적, 납품 처리 | 전체 접근 |
| **재무팀** | 송금, 수입원가정산, 지출결의서 접수/재고대상 판정, 문서 발행 | 전체 접근 |
| **대표이사** | 발주 결재 승인, 주요 계약 결재, 고액 지출결의서 결재 | 전체 접근 |

> **구매/회계**: 영업팀 + 재무팀 실무 협업, 대표이사 결재
> **국내 구매**: 전 사원 지출결의서 상신 → 부서 결재 → 재무팀 접수/재고판정
> **재고**: 전 부서 공용 — 구매(해외/국내) 입고 시 연동 (재무팀 판정 기반)

---

## 6. 업무 흐름 (Workflow)

### 6.1 해외 구매 흐름 (영업팀 + 재무팀)

```
[1] 장비 마스터 등록 (영업팀)
    └→ manufacturer + modelName unique 검증
    └→ 중복 시 기존 마스터 재사용

[2] 계약 등록 (영업팀)
    └→ 계약번호, 사업명, 고객사(납품 고객) 입력
    └→ 상태: ACTIVE

[3] 해외 발주 생성 (영업팀)
    └→ 계약 선택 → 제조사, 품목(마스터 참조), 통화, 금액
    └→ 상태: DRAFT
    └→ Phase 1: 직접 ORDERED로 전환
    └→ Phase 2+: 결재 상신 → PENDING_APPROVAL → 승인 시 APPROVED

[4] 발주 확정 → 해외 발주 + 송금 (영업팀 + 재무팀)
    └→ 해외 제조사에 발주 전달 (영업팀)
    └→ invoice#, due date, OA# 입력, 송금 처리 (재무팀)
    └→ 상태: ORDERED

[5] 제작 트래킹 (영업팀 담당자)
    └→ 제조사와 소통하며 제작 진행율 주기적 업데이트 (0~100%)
    └→ 진행 메모 기록 (예: "PCB 조립 완료, 테스트 진행 중")
    └→ 상태: IN_PRODUCTION
    └→ 담당자 대시보드에서 전체 발주 현황 트래킹

[6] 선적 (영업팀)
    └→ 진행율 100% 도달 → 선적 준비
    └→ 선적예정일 → 실제선적일 입력
    └→ 상태: SHIPPED

[7] 통관 (영업팀 + 재무팀)
    └→ 통관일, 통관담당자 입력
    └→ 관세업체로부터 수입면장(수입신고필증) 수령
    └→ 상태: CUSTOMS

[8] 입고 (영업팀 + 재무팀)
    └→ 입고일, 입고위치 입력 (영업팀)
    └→ 재무팀이 수입면장 기반으로 수입원가정산 작성 (송금액 + 관세/운송비 → 원가 산출)
    └→ 재고번호 자동 부여 (장비 마스터 + 시리얼 → 재고번호)
    └→ 재고 트랜잭션: "구매(입고)" 기록
    └→ 상태: ARRIVED

[9] 납품/출고 (영업팀)
    └→ 납품처, 프로젝트 입력
    └→ 재고 트랜잭션: "판매(출고)" 기록
    └→ 발주 상태: CLOSED
    └→ 계약 내 모든 발주 CLOSED 시 계약 상태: COMPLETED
```

### 6.2 국내 구매 흐름 — 지출결의서 (전 사원 → 부서 결재 → 재무팀)

```
[1] 지출결의서 작성 (전 사원)
    └→ 건명, 목적/사유, 품목(명/수량/단가), 공급사, 프로젝트(선택)
    └→ 견적서/영수증 등 증빙 첨부
    └→ 상태: DRAFT → 작성 완료 후 SUBMITTED (결재 상신)

[2] 부서 결재 (결재라인 — 부서장/대표이사)
    └→ 기존 결재라인(ApprovalLine)을 통해 승인/반려
    └→ 승인: 상태 → APPROVED
    └→ 반려: 상태 → REJECTED (사유 기재, 수정 후 재상신 가능)

[3] 재무팀 접수 (재무팀)
    └→ 결재 완료된 지출결의서가 재무팀 대시보드에 자동 표시
    └→ 재무팀 담당자가 접수 처리
    └→ 상태: FINANCE_RECEIVED

[4] 재고 대상 판정 (재무팀)
    └→ 재무팀이 지출 내역을 검토하여 재고관리 대상 여부 결정
    └→ 판정 기준: 금액, 자산 성격, 프로젝트 투입 여부 등
    └→ isInventoryTarget = true → 재고 대상 (입고 대기)
    └→ isInventoryTarget = false → 재고 미대상 (소모품 등) → 바로 COMPLETED
    └→ 상태: INVENTORY_DECIDED

[5] 입고 + 재고번호 부여 (재고 대상인 경우)
    └→ 제품 도착 시 입고일, 입고위치 입력
    └→ 재고번호 자동 부여 (장비 마스터 연결 또는 신규 등록)
    └→ InventoryItem 생성 + 지출결의서 연결 (sourceType: EXPENSE_REQUEST)
    └→ 재고 트랜잭션: "구매(입고)" 기록
    └→ 상태: ARRIVED → COMPLETED
```

### 6.3 재고 공통 흐름 (전 부서)

```
[1] 재고 조회 (전 사원)
    └→ 분류/위치/상태별 필터
    └→ 전체 재고 현황 확인

[2] 창고 이동 (전 사원)
    └→ 재고 트랜잭션: "창고(이동)" 기록
    └→ currentLocation 갱신

[3] 출고/반납 (전 사원)
    └→ 사용 목적, 프로젝트 입력
    └→ 재고 트랜잭션: "출고" 또는 "반납" 기록
```

### 6.4 추가비용 등록 흐름

```
[1] 재고 상세 → "비용 추가" (전 사원)
    └→ 유형 선택: AS | 업그레이드 | 부품추가 | 모듈추가 | 수리 | 기타
    └→ 비용 항목명, 금액, 업체, 일자, 증빙 첨부
    └→ 외화 결제 시: 통화, 외화금액, 환율 입력

[2] 저장 시 자동 처리
    └→ AssetCostEvent 생성
    └→ InventoryItem.totalAdditionalCost += cost
    └→ InventoryItem.totalCostOfOwnership 재계산

[3] 비용 이력 조회 (재고 상세 > 비용 이력 탭)
    └→ 초기 원가 (수입원가정산 / 구매가)
    └→ 추가비용 목록 (날짜순, 유형별 필터)
    └→ TCO = 초기원가 + 추가비용 합계
```

---

## 7. 기능 목록 (Feature List)

### P0 (핵심 — MVP)

| 기능 | 영역 | 설명 | 우선순위 |
|------|------|------|----------|
| 장비 마스터 CRUD | 공용 | 장비 종류 등록, 중복 방지 (manufacturer+model unique) | P0 |
| 계약 CRUD | 구매/회계 | 사업 단위 계약 등록, 발주와 1:N 연결 | P0 |
| 해외 발주 CRUD | 구매/회계 | 발주 생성/수정/조회, 다통화(EUR/GBP/USD), 상태 관리 | P0 |
| 발주 상태 FSM | 구매/회계 | DRAFT→ORDERED→IN_PRODUCTION→SHIPPED→CUSTOMS→ARRIVED→CLOSED | P0 |
| 제작 진행율 트래킹 | 구매/회계 | 담당자가 진행율(0~100%) 업데이트 + 메모 + 이력 타임라인 | P0 |
| 발주 현황 대시보드 | 구매/회계 | 담당자별 발주 현황 매트릭스 + 통화별 합계 + 경고 | P0 |
| 지출결의서 CRUD | 구매/회계 | 전 사원 지출결의서 작성→부서결재→재무접수→재고판정→입고 | P0 |
| 수입원가정산 CRUD | 구매/회계 | 송금액 + 부대비용 입력, 원가 자동 계산 | P0 |
| 재고 목록/조회 | 재고 | 전체 재고 현황, 분류/위치별 필터 (전 사원 접근) | P0 |
| 입고 처리 | 재고 | 해외/국내 구매 입고 시 재고번호 자동 생성 | P0 |
| 재고 트랜잭션 | 재고 | 구매(입고)/창고(이동)/출고/반납 이력 기록 | P0 |
| 자산 추가비용 등록 | 재고 | AS/업그레이드/부품추가 등 추가비용 이력 등록, TCO 자동 계산 | P0 |
| 역할별 메뉴 분리 | 공용 | 구매/회계 탭(재무+영업), 재고 탭(전 사원) | P0 |
| 범용 전자결재 프레임워크 | 공용 | 양식 관리+동적 폼+N단 결재 FSM+후처리 훅+대기함/상신함 UI | P0 |
| 결재 양식 초기 등록 (18종) | 공용 | ecount 기준 양식 시드 데이터 | P0 |
| 파일 첨부 | 공용 | 영수증/세금계산서 등 증빙자료 첨부 (다중 파일) | P0 |
| 스마트폰 스캔 | 공용 | 카메라 촬영→이미지 최적화(리사이징+압축)→첨부 | P0 |

### P1 (중요)

| 기능 | 영역 | 설명 | 우선순위 |
|------|------|------|----------|
| 문서 발행 시스템 | 구매/회계 | 입력 데이터 → 양식별 PDF 생성 + 이메일 발송/인쇄 | P1 |
| 발주서 발행 | 구매/회계 | 발주 데이터 → 발주서 양식 PDF (제조사 발송용) | P1 |
| 수입원가정산서 발행 | 구매/회계 | 원가정산 데이터 → 정산서 양식 PDF (사내 보관/재무팀용) | P1 |
| 거래명세서 발행 | 구매/회계 | 납품 시 거래명세서 양식 PDF (고객사 발송용) | P1 |
| 회계감사 자료 출력 | 구매/회계 | 기간별 구매/원가정산/재고 내역 종합 보고서 PDF/엑셀 출력 | P1 |
| 재고 실사 기능 | 재고 | 실사 계획 → 현장 확인(모바일) → 차이 보고서 생성 | P1 |
| 발주 대시보드 고도화 | 구매/회계 | 통장잔고 입력, 추가필요금액 자동계산, 기간별 추이 차트 | P1 |
| 발주-원가정산-재고 연결 | 구매/회계 | 발주에서 원가정산 → 재고까지 원클릭 추적 | P1 |
| 재고 분류별 소계 | 재고 | 미착품/상품/원재료 등 분류별 금액 합계 | P1 |
| 근태 결재 마이그레이션 | 공용 | 휴가/OT 결재를 범용 프레임워크로 이관 (internal API 콜백) | P1 |

### P2 (개선)

| 기능 | 영역 | 설명 | 우선순위 |
|------|------|------|----------|
| 회계시스템 체크리스트 | 구매/회계 | 입고 시 회계 입력 항목 안내 + 완료 체크 | P2 |
| 환율 자동 조회 | 구매/회계 | 송금 시점 환율 자동 입력 (API) | P2 |
| 엑셀 데이터 마이그레이션 | 공용 | 기존 3개 엑셀 → DB 일괄 이관 | P2 |
| 발주 엑셀 내보내기 | 구매/회계 | 기존 양식과 동일한 형태로 엑셀 다운로드 | P2 |

---

## 8. 기술 결정 (Technical Decisions)

### 8.1 서비스 배치

**결정: equipment-service 내 확장**

이유:
- 재고번호가 장비 관리(Equipment)와 직접 연결
- 기존 Part(부품), PurchaseOrder(발주) 모델과 구조 유사
- 수입원가는 수리관리의 RepairCost와 유사한 패턴
- 별도 서비스 불필요 (같은 DB 스키마)

### 8.2 UI 배치

**결정: 상단 탭 2개 분리 — 역할별 접근 제어**

```
AppLayout 상단 탭:
[프로젝트] [근태] [장비] [수리관리] [구매/회계] [재고] [결재] ← 신규 3개
                                    ↑ 재무+영업만   ↑ 전사원  ↑ 전사원
```

구매/회계 페이지 (재무팀 + 영업팀):
```
apps/web/src/app/procurement/
├── layout.tsx                 # 서브탭 레이아웃
├── page.tsx                   # 해외 발주 목록 (메인)
├── [id]/page.tsx              # 발주 상세
├── contracts/page.tsx         # 계약 목록
├── contracts/[id]/page.tsx    # 계약 상세 (하위 발주 목록)
├── expenses/page.tsx           # 지출결의서 목록 (재무팀: 접수/판정)
├── expenses/[id]/page.tsx     # 지출결의서 상세
├── masters/page.tsx           # 장비 마스터 목록
├── settlements/page.tsx       # 수입원가정산 목록
└── settlements/[id]/page.tsx  # 원가정산 상세
```

재고 페이지 (전 사원):
```
apps/web/src/app/inventory/
├── layout.tsx              # 서브탭 레이아웃
├── page.tsx                # 재고 목록 (메인)
├── [id]/page.tsx           # 재고 상세 (탭: 기본정보, 입출고이력, 비용이력, TCO)
└── transactions/page.tsx   # 입출고/이동 이력
```

**재고 상세 페이지 탭 구성**:
- **기본정보**: 재고번호, 품목, 시리얼, 위치, 상태
- **입출고 이력**: InventoryTransaction 목록
- **비용 이력**: 초기 원가 + AssetCostEvent 목록 (AS/업그레이드/부품추가 등)
- **TCO 요약**: 초기원가 + 추가비용 합계 = 총 보유비용, 비용 유형별 차트

### 8.3 재고번호 생성

- DB 시퀀스 `inventory_seq` 사용 (5자리, 0패딩)
- 형식: `E{seq}_{itemIndex}` (예: E02300_1)
- unique 제약: `inventoryNo` 컬럼

### 8.4 다통화 처리

- 발주: 통화(EUR/GBP/USD) + 외화금액 저장
- 원가정산: 외화금액 + 환율 → 원화금액 자동 계산
- 재고: 원화(KRW) 기준 단가/금액

### 8.5 문서 발행 시스템

**방식**: 서버사이드 PDF 생성 + 브라우저 인쇄/이메일 발송

**발행 가능 문서**:

| 문서 | 데이터 소스 | 용도 | 발송 대상 |
|------|-------------|------|-----------|
| 발주서 (Purchase Order) | OverseasOrder + items | 해외 제조사에 발주 요청 | 제조사 (이메일) |
| 수입원가정산서 | ImportCostSettlement | 사내 원가 기록/보관 | 재무팀 (인쇄/보관) |
| 거래명세서 | 납품 정보 + 재고 품목 | 고객사 납품 시 첨부 | 고객사 (이메일/인쇄) |

**기능**:
- PDF 미리보기 → 인쇄 또는 이메일 발송 선택
- 이메일 발송: 수신자 입력 → 첨부파일(PDF)로 발송
- 인쇄: 브라우저 인쇄 다이얼로그
- 발행 이력 기록 (언제, 누가, 어디로 발송했는지)

### 8.6 파일 스토리지 + 이미지 최적화

- **스토리지**: Docker 볼륨 마운트 (uploads/ 디렉토리)
- **최적화 라이브러리**: sharp (Node.js)
- **이미지 처리 규칙**:
  - 장변 최대 2000px 리사이징 (문서 판독 충분)
  - JPEG 품질 80% 압축
  - 원본 대비 약 70~90% 용량 절감
  - PDF 파일은 원본 유지
- **파일 제한**: 단일 파일 최대 10MB, 건당 첨부 최대 10개
- **스마트폰 스캔**: HTML5 `<input type="file" accept="image/*" capture="environment">`

### 8.7 범용 전자결재 프레임워크

- **서비스 배치**: **별도 approval-service (Port 3006)** 신규 생성
  - 이유: 결재 문서량(연 ~10,000건) + 파일 첨부 + PDF 생성 + 18종 양식이 auth-service 범위 초과
  - 결재선(ApprovalLine)은 auth-service에 유지, approval-service가 내부 API로 조회
  - FileService(업로드/이미지 최적화)도 approval-service에 포함
- **결재 FSM**: DRAFT → [합의] → SUBMITTED → STEP_N_PENDING → APPROVED | REJECTED (N단 가변)
- **양식 시스템**: ApprovalTemplate (동적 필드 정의 JSON) → 양식 추가 시 코드 변경 없음
- **후처리 훅**: postApprovalAction으로 업무별 자동 동작 (FINANCE_FORWARD, ORDER_CONFIRM 등)
- **결재선 연동**: 기존 ApprovalLine (3단+위임) 재사용, 양식별 규칙(고정/금액기준) 확장
- **근태 마이그레이션**: Phase 8에서 휴가/OT를 프레임워크 경유로 전환 (internal API 콜백)

### 8.8 알림 시스템

- **결재 알림**: 결재 요청/승인/반려/합의요청 시 대상자에게 알림
- **참고 전달 알림**: 결재 완료 후 참고부서/담당자에게 문서 전달 알림
- **발주 경고**: 선적예정일 경과, 통관 지연 시 담당자 알림
- **UI 표시**: AppLayout 헤더에 결재 대기 건수 배지 + 알림 드롭다운
- **구현 방식**: 기존 notifications 테이블 활용 (auth-service activity 이벤트 패턴 재사용)
  - Phase 1: DB 폴링 기반 (알림 테이블 + 주기적 조회)
  - 향후: WebSocket 실시간 알림 (고도화)

### 8.9 API 프록시 설정 (next.config.mjs)

Phase별 추가 리라이트 규칙:
```
Phase 1: /api/procurement/** → equipment-service:3005 (기존 서비스 확장)
Phase 2: /api/approval/** → approval-service:3006 (신규)
Phase 3: /api/inventory/** → equipment-service:3005 (기존 서비스 확장)
```

---

## 9. 구현 순서 (Implementation Phases)

### Phase 1: 장비 마스터 + 계약 + 해외 발주 + 발주 트래킹

1. Prisma 스키마 추가 (ProductMaster, Contract, OverseasOrder, OverseasOrderItem, OrderProgressLog)
2. Backend: ProductMasterService, ContractService, OverseasOrderService
3. Backend: OrderProgressService (진행율 업데이트, 이력 조회)
4. Backend: Routes 등록 + 발주 통계 API (담당자별/통화별 합계)
5. Frontend: AppLayout "구매/회계" + "재고" 탭 추가 (역할별 접근 제어)
6. Frontend: 장비 마스터 목록/등록
7. Frontend: 계약 목록/상세
8. Frontend: 해외 발주 목록/상세 (상태 FSM + 제작 진행율 바)
   └→ 발주 상세: 진행율 업데이트 UI + 이력 타임라인
   └→ 상태별 색상 코딩 (엑셀 대체)
   └→ 선적예정일 경과/통관 지연 경고 표시
9. Frontend: 발주 현황 대시보드 (담당자 메인 화면)
   └→ 내 발주 현황: 상태별 × 통화별 매트릭스
   └→ 통화별 발주 합계 (EUR/GBP/USD)
   └→ 전체 담당자 보기 (관리자용)

### Phase 2: 범용 전자결재 프레임워크 + 파일 첨부

10. **approval-service 신규 생성** (Port 3006, Docker Compose 추가)
11. Prisma 스키마 추가 (ApprovalTemplate, ApprovalDocument, ApprovalStep, Attachment, ExpenseFollowUp)
12. Backend: ApprovalTemplateService (양식 CRUD, 시드 데이터 18종 등록)
13. Backend: ApprovalDocumentService (문서 생성, 상신, N단 결재 FSM, 후처리 훅)
    └→ submit(): 합의자 확인 → 결재선 자동 조회 → ApprovalStep N개 생성 → STEP_1_PENDING
    └→ approve(): 현재 단계 승인 → 다음 단계 or 최종 APPROVED
    └→ reject(): 현재 단계 반려 → REJECTED
    └→ onApproved(): postApprovalAction 실행 + 참고부서/담당자 전달
14. Backend: FileService (파일 업로드, 이미지 최적화 — sharp)
    └→ 이미지: 장변 최대 2000px 리사이징 + JPEG 80% 압축
    └→ PDF: 원본 유지
    └→ 저장: 로컬 스토리지 (uploads/ 디렉토리)
15. next.config.mjs: /api/approval/** → approval-service:3006 리라이트 추가
16. Frontend: AppLayout "결재" 탭 추가 + 대기함 배지 카운트
17. Frontend: 결재 대기함, 상신함, 참조함(CC + 참고 전달), 완료함
18. Frontend: 새 결재 작성 (양식 선택 → 결재 단계 선택 → 동적 폼 → 참고부서/담당자 → 첨부 → 상신)
19. Frontend: 결재 상세 (단계 타임라인, 승인/반려/의견)
20. Frontend: 파일 첨부 컴포넌트 (드래그앤드롭 + 카메라 스캔)
    └→ 스마트폰: <input type="file" capture="environment">
    └→ PC: 파일 선택 또는 드래그앤드롭
21. Frontend: 양식 관리 페이지 (ADMIN)
22. 발주서 결재 연동 (발주 생성 → ApprovalDocument 자동 생성, 승인 시 ORDER_CONFIRM)

### Phase 3: 재고 시스템 + 추가비용 추적 (전 사원 접근)

23. Prisma 스키마 추가 (InventoryItem, InventoryTransaction, AssetCostEvent)
24. Backend: InventoryService (재고번호 생성, 트랜잭션)
25. Backend: AssetCostService (추가비용 CRUD, TCO 자동 계산)
26. Frontend: 재고 목록 (전 사원), 입고/이동/출고 처리
27. Frontend: 재고 상세 — 비용 이력 탭 (AS/업그레이드/부품추가 등록, TCO 표시)
28. 해외 발주 입고 시 재고 자동 생성 연동 (초기 원가 = 수입원가정산 금액)
29. 출고신청서 결재 연동 (ApprovalDocument template: INVENTORY_RELEASE → 재고 출고 트랜잭션)

### Phase 4: 지출결의서 (국내 구매)

30. Prisma 스키마 추가 (ExpenseFollowUp — 결재 후속 처리 데이터)
31. Backend: ExpenseFollowUpService (재무접수, 재고판정, 입고)
    └→ 결재는 범용 프레임워크 사용 (ApprovalDocument template: EXPENSE)
    └→ postApprovalAction: FINANCE_FORWARD → ExpenseFollowUp 자동 생성 + 재무팀 대시보드 표시
32. Frontend: 지출결의서 작성 (전 사원) — 결재 프레임워크 "새 결재"에서 EXPENSE 양식 선택
33. Frontend: 재무팀 대시보드 — 접수 대기 목록, 재고대상 판정 UI
34. 재고판정 시 입고 → 재고번호 자동 부여

### Phase 5: 수입원가정산

35. Prisma 스키마 추가 (ImportCostSettlement, CostRemittance, CostDuty, CostExtra, CostItem)
36. Backend: ImportCostService (원가 자동 계산, 부대비용 귀속)
37. Frontend: 원가정산 양식 UI
38. 발주-원가정산-재고 연결 (원가정산 완료 시 재고 초기원가 자동 갱신)

### Phase 6: 문서 발행 + 회계감사 자료

39. Backend: PDF/엑셀 생성 서비스 (발주서, 원가정산서, 거래명세서, 결재 문서)
    └→ 결재 문서 PDF: approvalStepCount에 따른 결재란 동적 렌더링
40. Backend: 이메일 발송 서비스 (PDF 첨부)
41. Frontend: PDF 미리보기 + 인쇄/이메일 발송 UI
42. 발행 이력 기록
43. 회계감사 보고서 출력 (구매내역, 원가정산, 재고현황, 입출고명세, 결재이력, TCO 보고서)

### Phase 7: 재고 실사

44. Prisma 스키마 추가 (InventoryAudit, InventoryAuditItem)
45. Backend: InventoryAuditService (실사 생성, 대상 자동 로드, 차이 계산)
46. Frontend: 실사 계획/현장 입력 UI (모바일 최적화)
47. Frontend: 차이 보고서 생성 + PDF/엑셀 출력
48. 차이 조정 기능 (승인 후 시스템 수량 보정)

### Phase 8: 근태 결재 마이그레이션

49. attendance-service에 internal API 추가
    └→ POST /internal/leave/:id/framework-approve (프레임워크에서 호출)
    └→ POST /internal/overtime/:id/framework-approve
50. 휴가신청 → ApprovalDocument (template: LEAVE) 생성으로 전환
51. OT신청 → ApprovalDocument (template: HOLIDAY_WORK) 생성으로 전환
52. 기존 LeaveService.approve/reject → 프레임워크 경유로 래핑
53. 팀 근태 페이지(/me/team) 승인 UI → 결재 대기함으로 통합
54. 기존 직접 approve/reject API → deprecated 처리

### Phase 9: 고도화

55. 발주 대시보드 고도화 (통장잔고 입력, 추가필요금액 자동계산, 기간별 추이 차트)
    └→ 기본 대시보드는 Phase 1에서 구현 완료, 여기서는 고급 기능 추가
56. 재고 분류별 소계 + TCO 분석 (추가비용 유형별 통계)
57. 회계 체크리스트 (입고 시 회계시스템 입력 항목 안내 + 완료 체크)
58. M/S 일괄서명 (다건 결재 일괄 처리)

---

## 10. 비기능 요구사항

| 항목 | 기준 |
|------|------|
| 발주 목록 조회 | < 500ms |
| 재고번호 유일성 | DB unique + 시퀀스 |
| 금액 정밀도 | Decimal(12,2) / 환율 Decimal(10,4) |
| 다통화 | EUR, GBP, USD, KRW |
| 회계시스템 | 외부 시스템 (연동 불가, 체크리스트 제공) |
| 첨부파일 제한 | 단일 10MB, 건당 최대 10개 |
| 이미지 최적화 | 장변 2000px, JPEG 80% |
| 파일 스토리지 | ~10GB/년 예상 (디스크 여유 확보 필요) |
| 신규 서비스 | approval-service (Port 3006) — Docker Compose 추가 |

### 예상 데이터 규모

| 데이터 | 연간 건수 | 5년 누적 | 비고 |
|--------|-----------|----------|------|
| 재고 (InventoryItem) | 수만 건 | ~10만 건 | 인덱스: inventoryNo, category, currentLocation |
| 계약 (Contract) | ~1,000건 | ~5,000건 | |
| 구매 (해외+국내) | ~10,000건 | ~50,000건 | |
| 첨부파일 | ~10,000건 | ~50,000건 | ~10GB/년 (최적화 후) |
| 결재 | ~10,000건 | ~50,000건 | 구매 건수와 연동 |

---

## 11. 리스크 및 대응 방안

### 11.1 아키텍처 리스크

| # | 리스크 | 심각도 | 현황 | 대응 방안 | 시점 |
|---|--------|:------:|------|-----------|------|
| R-1 | **next.config.mjs 리라이트 폭발** — 현재 59줄, 리소스당 2줄 패턴. 구매/재고/결재 API 추가 시 100줄+ | High | 30+ 라우트, 캐치올(`/api/v1/:path*` → project-service) 순서 실수 위험 | 서비스 단위 와일드카드로 리팩토링: `/api/v1/procurement/:path*` → equipment-service, `/api/v1/approval/:path*` → approval-service. 리소스별 2줄 → **서비스별 2줄**로 축소 | Phase 1 시작 전 |
| R-2 | **서비스 간 통신 복잡도** — approval-service가 auth/equipment/attendance 모두와 통신. postApprovalAction 후처리 실패 시 결재 승인↔비즈니스 불일치 | High | 현재 서비스 간 통신 단순 (auth→user 정도) | ① 후처리 실패 시 Redis 기반 재시도 큐 ② 실패 로그 + 수동 재처리 UI ③ 향후 RabbitMQ 이벤트 기반 비동기 전환 검토 | Phase 2 설계 |
| R-3 | **역할/권한 4종 한계** — UserRole(ADMIN/MANAGER/OPERATOR/VIEWER)로 영업팀/재무팀/부서장/대표이사 구분 불가 | Medium | 직급-권한 Plan(90%)에서 부분 해결 예상 | 부서(Department) 기반 권한 체크 로직 추가. "재무팀 소속 = 재무 권한" 패턴. 기능별 Permission 필요 시 직급-권한 구현 확인 후 결정 | Phase 1 시작 전 확인 |
| R-4 | **approval ↔ auth 크로스 스키마** — Prisma 크로스-스키마 JOIN 불가. 결재 시마다 auth-service HTTP 호출 필요 | Medium | 서비스별 스키마 분리 (auth=public, equipment, attendance, project) | 결재선 **스냅샷 패턴**: 문서 생성 시 결재자 정보를 ApprovalStep에 복사 저장 (approverName, roleName 이미 설계됨). 매 결재마다 auth 호출 최소화 | Phase 2 설계 반영 완료 |
| R-5 | **equipment 스키마 비대화** — 현재 763줄/26모델. 구매/재고 ~15모델 추가 시 40+ 모델, 1000줄+ | Medium | 장비+수리관리로 이미 큰 편 | 코드 디렉토리만 분리 (`src/procurement/`, `src/inventory/`). 스키마는 equipment 유지. 향후 procurement-service 분리 가능성 고려한 구조 | Phase 1 구현 시 |

### 11.2 인프라 리스크

| # | 리스크 | 심각도 | 현황 | 대응 방안 | 시점 |
|---|--------|:------:|------|-----------|------|
| R-6 | **Docker 컨테이너 증가** — 현재 9개 → approval-service 추가 시 10개 | Low | 메모리 1.7GB/15.2GB (11%), CPU 28% (web dev 폴링) | 하드웨어 충분 (Ryzen 7 9800X3D, 32GB RAM, SSD 669GB 여유). 모니터링하며 진행 | 모니터링 |
| R-7 | **PostgreSQL 커넥션 풀** — 서비스 10개가 각각 커넥션 풀 사용 | Low | 기본 max_connections=100, 서비스당 ~10 커넥션 | 현재 규모에서 문제 없음. 서비스 증가 시 `DATABASE_POOL_SIZE` 환경변수로 조정 | 모니터링 |
| R-8 | **파일 스토리지 장기 누적** — 첨부파일 연 ~10GB × 5년 = 50GB | Low | SSD 669GB 여유, HDD 2TB 별도 존재 | SSD에서 운영, 장기 보관분은 HDD 2TB로 `uploads/archive/` 이관 정책 수립 | Phase 6 이후 |

### 11.3 하드웨어 현황 (2026-04-10 기준)

```
CPU:  AMD Ryzen 7 9800X3D (8코어/16스레드)
RAM:  32GB (Docker 15.2GB 할당)
SSD:  1TB (사용 263GB / 여유 669GB)
HDD:  2TB (데이터 백업/보관용)

Docker 리소스 사용 (10개 컨테이너):
├── 메모리: 1.7GB / 15.2GB (11%) — 여유 88%
├── CPU: ~28% (대부분 web dev 핫리로드 폴링)
└── 디스크: Docker 이미지 23.5GB + 볼륨 0.5GB

→ 판정: 하드웨어 제약 없음. 모든 리스크는 소프트웨어 아키텍처 수준.
```

### 11.4 리스크 대응 우선순위

```
Phase 1 시작 전 (선행 작업):
├── R-1: next.config.mjs 서비스 단위 와일드카드 리팩토링
├── R-3: 역할/권한 — 직급-권한 구현 상태 확인, 부서 기반 권한 체크 설계
└── R-5: equipment-service 코드 디렉토리 분리 구조 결정

Phase 2 설계 시 반영:
├── R-2: 후처리 실패 재시도 전략 + 수동 재처리 UI
└── R-4: 스냅샷 패턴 (이미 설계 반영 완료)

모니터링 (문제 발생 시 대응):
├── R-6: Docker 리소스
├── R-7: DB 커넥션 풀
└── R-8: 파일 스토리지 누적
```

---

## 12. 용어 정리

| 용어 | 설명 |
|------|------|
| 장비 마스터 (ProductMaster) | 장비 종류 카탈로그 (모델+제조사 기준) |
| 계약 (Contract) | 사업 단위 계약, 하나의 계약에 여러 발주 포함 (1:N) |
| 해외 발주 (OverseasOrder) | 계약 하위의 개별 해외 제조사 구매 건 |
| 수입면장 (수입신고필증) | 관세업체에서 수령하는 통관 서류 (신고번호, 관세 등 기재) |
| 수입원가정산 (ImportCostSettlement) | 재무팀이 수입면장 기반으로 작성하는 원가 계산서 (송금액+관세+운송비) |
| 재고번호 (inventoryNo) | E{5자리}_{순번} 형식의 유일 식별자 |
| 지출결의서 | ApprovalDocument(EXPENSE 양식) + ExpenseFollowUp 조합으로 구현 |
| ExpenseFollowUp | 결재 승인 후 재무팀 후속 처리 데이터 (접수/재고판정/입고) |
| 재고 대상 판정 | 재무팀이 지출결의서 검토 후 재고관리 대상(자산) vs 비대상(소모품) 결정 |
| 재고 트랜잭션 | 구매(입고) / 창고(이동) / 판매(출고) |
| 미착품 | 발주했으나 아직 입고되지 않은 품목 |
| OA# | Open Account 번호 (외상거래 번호) |
| AWB | Air Waybill (항공화물운송장) |

---

## 13. 참고 자료

- 발주_인보이스.xlsx — 현재 발주 관리 양식 (48행, EUR/GBP/USD 섹션)
- 수입원가정산.xlsx — 수입원가 계산 양식 (건당 45행 반복)
- 재고합본.xlsx — 재고 현황 (분류: 미착품/상품/원재료/전기상품/전기원재료)
- 기존 equipment-service PurchaseOrder 모델 — 국내 부품 발주 (별도)
