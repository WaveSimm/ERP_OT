# ecount → ERP 재고 마이그레이션 결과 보고서

> **실행일**: 2026-05-18
> **대상**: ecount 4개월(2026-01-01 ~ 04-30) + 거래처 마스터
> **검토안**: `ecount-재고-마이그-검토안.md`
> **스크립트**: `scripts/import-ecount-inventory.py`

---

## 1. 결과 요약

### 1.1 처리량
| 영역 | 변경량 |
|---|---:|
| Phase A 표기 통일 | **1971 row UPDATE** |
| Phase B-1 마스터 갱신 | **1448 row** (1125 customers + 122 locations + 305 products) |
| Phase B-2 트랜잭션 import | **2009 row** (509 inventory + 935 transactions + 365 customer_assets) |
| **합계** | **~5400 row 처리** |

### 1.2 테이블별 Before/After
| 모델 | Before | After | 증가 |
|---|---:|---:|---:|
| customers | 157 | **1178** | +1021 |
| storage_locations | 100 | **222** | +122 |
| product_masters | 759 | **1064** | +305 |
| inventory_items | 1572 | **2081** | +509 |
| inventory_transactions | 2983 | **3918** | +935 |
| customer_assets | 331 | **696** | +365 |

---

## 2. Schema 변경

| 테이블 | 컬럼 | 용도 |
|---|---|---|
| `customers` | `legacy_ecount_id TEXT` | ecount A코드 (`A00446` 등) 보존 |
| `product_masters` | `legacy_ecount_id TEXT` | ecount 품목코드 (창고이동, 171 unique) cross-ref |

→ 다음에 Prisma `db pull` 또는 `schema.prisma` 수동 동기화 권장.

---

## 3. Phase A 표기 통일 결과

### 변환 규칙
- `E#####_#` → `#E#####_#` (해시 prefix)
- `NNNNN` → `#NNNNN` (그룹웨어 해시)
- 영향 테이블: `inventory_items`, `cost_items`, `repair_orders`
- 변경 대상 외(SKIP): `INV-YYMM-NNNN` (신규 ERP), placeholder (`미정`, `미생성`, `무통관` 등)

### 결과
| 테이블·컬럼 | 변경 row |
|---|---:|
| inventory_items.inventory_no (E#####_#) | 1401 |
| inventory_items.inventory_no (NNNNN) | 162 |
| cost_items.inventory_no | 405 |
| repair_orders.otInventoryNo | 3 |
| **합계** | **1971** |

---

## 4. Phase B-1 마스터 갱신 결과

### B-1a customers
- INSERT 1021 (신규 거래처)
- UPDATE 104 (매칭된 ERP customers에 정식 이름·businessNo·legacy_ecount_id 보강)
- ERP-only 53 (그대로 유지 — 옛 거래처)
- businessNo 채워진 row: 555 (정상 사업자번호)
- legacy_ecount_id 채워진 row: 570 (A코드)

### B-1b storage_locations
- 내부 OT_* → type=WAREHOUSE (60 신규)
- 외부 거래처/사람 → type=CUSTOMER (62 신규)
- 매칭 51 SKIP (이미 있음)

### B-1c product_masters
- INSERT 305 (신규 품목)
- SKIP 단일 매칭 57 / 복수 매칭 6
- legacy_ecount_id 채워진 row: 141 (창고이동 품목코드 cross-ref)

---

## 5. Phase B-2 트랜잭션 import 결과

### inventory_items 신규 509 분류
| 형식 | 건수 | 의미 |
|---|---:|---|
| `#E#####_#` | **379** | ecount 4개월 적요 (예상 378과 일치) |
| `#NNNNN` | **127** | 그룹웨어 해시 (창고이동 단독 — 과거 보유 자산) |
| 기타 (`#741`, `#617`, `#684`) | 3 | 짧은 표기 (자리수 예외) |

### inventory_transactions +935
| Type | 건수 |
|---|---:|
| PURCHASE | 336 |
| TRANSFER | 234 |
| RELEASE | 365 |

### customer_assets +365
- 모두 ecount 판매현황의 출고 자산
- `otInventoryNo` cross-ref로 inventory_items와 연결
- 고객사 매칭 실패: 0건 ✅

### Skip 처리
- inv_no 누락 row: 3건 (이름 없는 row 또는 헤더)

---

## 6. 검증 결과

### 6.1 정합성
| 검증 항목 | 결과 |
|---|---|
| inventory_no 중복 | **0건** ✅ |
| ecount 521 unique 적요 ↔ ERP inventory_no | **521 정확 매칭** ✅ |
| 고객사 매칭 실패 | 0건 ✅ |
| InventoryItem.id 충돌 | 0건 ✅ |

### 6.2 카테고리·상태 분포 (최종)
```
IN_TRANSIT  × RELEASED  : 709
PREV_PRODUCT× IN_STOCK  : 401
PRODUCT     × RELEASED  : 376
PREV_PRODUCT× RELEASED  : 237
PRODUCT     × IN_STOCK  : 147
IN_TRANSIT  × IN_STOCK  : 145
RAW_MATERIAL× RELEASED  : 39
...
```

---

## 6.3 동일 업체 통합 (저녁 추가)
- customers 1178 → **1159** (-19 row)
- 통합 14건 + 이름 구분 1건 + 분리 유지 2건 + 보류 1건
- 외래키 이동: customer_contacts 107 + customer_assets 35 + repair_orders 49

| 통합 그룹 | 효과 |
|---|---|
| **KHOA** 국립해양조사원 | 자산 113 + AS 14 단일화 |
| **KIOST** 한국해양과학기술원 | 자산 58 + AS 22 단일화 |
| **해양정보기술 MIT** | 자산 12 + AS 5 단일화 |
| KOPRI·KRISO·국립수산과학원·에스엠오션·한국지질자원연구원·한국항공우주산업·한국해양조사협회 등 | 자산·AS 통합 |
| 외국 회사 5건 (De Regt, Freewave, GARMIN, Kipp&Zonen, Rochester) | 표기 통일 |
| 한국 회사 2건 (엠에스엘테크놀로지, 엔쉽) | 사업자번호 + legacy 통합 |

분리 유지 (별개 법인 명확): 가나기업·대한과학·지오시스템·한국해양과학기술(사기업)
보류: Datem (사용자 결정 보류)
자사 오션테크는 별도 검토 (customer 등록 자체 의문)

## 7. 백업

| 백업 | 시점 | 크기 |
|---|---|---:|
| full-backup-20260518-082604.dump | 아침 (첫 백업) | 2.6MB |
| full-backup-20260518-194224-pre-phaseA.dump | Phase A 직전 | 2.6MB |
| full-backup-20260518-202555-pre-B2.dump | B-2 직전 | 2.7MB |

→ 단계별 롤백 가능. 메모리 `feedback_seed_preserve` 룰 부합.

---

## 8. 알려진 이슈·백로그

### 8.1 카운터 버그 (데이터 영향 없음)
- `scripts/import-ecount-inventory.py` 의 `n_upd_inv` 카운터가 purchase 분기에서만 카운트
- 실제 transfer/sale 매칭 UPDATE는 142건이나 보고는 1건
- **데이터는 정상 반영됨** (중복 0건, 전 트랜잭션 완료)

### 8.2 Phase A 잔여 백로그
- cost_items 복잡 표기 ~95건 (범위·콤마·placeholder)
- repair_orders 기타 17건 (`#E1234` 단순 형식, 범위 표기, placeholder)
- inventory_items 기타 4건 (`미정`, `미생성`, `E01403`, `무통관`)

### 8.3 Phase C — Cross-link 복구 (백로그)
- `inventory_items` ↔ `customer_assets` 시리얼·품목명 매칭
- 282 customer_assets serialNumber → inventory_items 매칭 가능
- 사용자 명시 요청 시 진행 ([[feedback_backlog_surfacing]])

---

## 9. 운영 가이드

### 9.1 신규 ecount 데이터 마이그
- 동일 스크립트 (`scripts/import-ecount-inventory.py`) 재사용
- 적요 `#E####_#` 형식 그대로 유지
- 새 거래처는 `legacy_ecount_id` 또는 `businessNo` 채워서 INSERT
- 새 품목은 `legacy_ecount_id` cross-ref 채워서 INSERT

### 9.2 AS 흐름
- AS 발생 시 자산 식별: 시리얼 또는 재고번호(`#E####_#`)로 `customer_assets.otInventoryNo` 조회
- `RepairOrder.customerAssetId` 자동 매핑 가능

### 9.3 단가·금액 조회
- 재고 트랜잭션에는 X
- ERP `OverseasOrder` / `ApprovalDocument` (지출결의서) 에서 조회
- ecount legacy 데이터는 별도 import 없이 ecount 외부 참조
