# 보안관리대장 (Security Management Register)

> **목적**: ERP·OT 플랫폼(사내 온프렘)의 보안 점검 결과·조치 현황·진행 기록을 지속 관리하는 살아있는 문서.
> **갱신 규칙**: 점검/조치/사고가 있을 때마다 ① 취약점 대장 상태 갱신 ② 하단 「진행 기록」에 날짜별 추가.
> **기준선**: KISA 「주요정보통신기반시설 기술적 취약점 분석·평가 상세가이드」(U-항목) + OWASP(`docs/04-operation/2026-05-02-OWASP-매핑.md`).
> **대상**: ERP_OT(`github.com/WaveSimm/ERP_OT`), ot-brain(`github.com/WaveSimm/ot-brain`), 호스트 `oceantech`(192.168.0.41, Ubuntu 26.04, 온프렘).

---

## 1. 보안 아키텍처 현황 (노출면)

- **외부(0.0.0.0) 리슨**: `22/SSH`, `3000/web` 단 2개 (2026-06-13 배치 A로 축소).
- 그 외 인프라·백엔드(postgres·redis·rabbitmq·ollama·ocr-engine·ocr-test-ui·서비스 3001~3008)는 **127.0.0.1 바인딩** → 외부 도달 불가. 컨테이너 간 통신은 docker 네트워크.
- ⚠️ **남은 핵심 노출**: SSH(비번 인증 ON)·web(평문 HTTP, LAN). → VPN + 방화벽 + 리버스프록시(TLS)로 잠그는 것이 다음 목표.
- 참고(외부 조언, 사내): 외부 포트 개방 금지·VPN 사용·SSH는 개발자 IP만·서버 네트워크 C클래스(서브넷) 분리.

---

## 2. 취약점 대장

상태: ✅완료 / 🔧진행 / ⬜미결 / 🟢양호(조치불요)

| ID | 항목 | 심각도 | 상태 | 조치/비고 |
|----|------|--------|------|-----------|
| V-01 | ocr `/dev/token` 무인증 ADMIN JWT 발급 | 🔴HIGH | ✅완료 | 라우트+화이트리스트 제거. commit `2b4338b` (2026-06-13) |
| V-02 | `import-ecount-inventory.py` DB 비번 하드코딩 | 🔴HIGH | ✅완료(코드) / ⬜미결(교체) | env(`ERP_DB_PASSWORD`)화 `2b4338b`. **노출된 기존 비번 교체 필요(외부)** |
| V-03 | 인프라·백엔드 포트 0.0.0.0 LAN 노출(Docker ufw 우회) | 🔴HIGH | ✅완료 | 127.0.0.1 바인딩. commit `767de16` (2026-06-13) |
| V-04 | ocr-service CORS `*` 와일드카드 | 🟠MED | ✅완료 | `${CORS_ORIGIN}`로 정정 `767de16` |
| V-05 | 운영 서버 `NODE_ENV=development` (secure 쿠키 꺼짐) | 🔴HIGH | ⬜미결 | **HTTPS(리버스프록시 TLS) 선행 후** production 전환 |
| V-06 | SSH `PasswordAuthentication=yes` (무차별 대입) | 🔴HIGH | ⬜미결 | 키 인증 전환(락아웃 주의 절차 필요) |
| V-07 | SSH `PermitRootLogin prohibit-password` | 🟠MED | ⬜미결 | `no`로(KISA U-01) |
| V-08 | SSH `X11Forwarding yes` | 🟠MED | ⬜미결 | `no` |
| V-09 | 방화벽 미설정(SSH 전체·web 전체 허용) | 🔴HIGH | ⬜미결 | ufw: SSH=개발IP만, web=VPN/내부대역만, DOCKER-USER로 3000 제한 |
| V-10 | IDOR — approval 문서 조회/수정/상신 | 🔴HIGH | ✅조치완료(2026-06-20, PR#71) | canAccess(조회=관계자)·isOwner(수정/상신=기안자) 검증 추가, 위반 403 |
| V-11 | IDOR — project 조회/gantt | 🟠MED | 🟢정책수용(2026-06-20) | 전 직원 조회 공개 정책 결정(사내 협업도구, 개인민감정보 아님). 수정/삭제는 기존 requireRole(ADMIN,MANAGER) 보호. 코드변경 없음 |
| V-12 | approval 커스텀 internalToken(미설정시 우회) | 🟠MED | ⬜미결 | 공유 `require-internal` 사용 |
| V-13 | auth seed 기본 admin 비번 `Admin1234!` | 🟠MED | ⬜미결 | env 없으면 throw |
| V-14 | knowledge-api 자산 approve/merge/delete 역할게이트 없음 | 🟠MED | ✅조치완료(2026-06-20, ot-brain PR#13) | preHandler가 /auth/me role을 req.userRole에 보존, approve·merge·confirm에 denyAssetWrite(ADMIN/MANAGER·관리토큰)→403 |
| V-15 | project collab SVG 업로드 허용(저장형 XSS) | 🟠MED | ⬜미결 | `image/svg+xml` 제거 |
| V-16 | 다운로드 3곳 `inline`+사용자 Content-Type | 🟠MED | ⬜미결 | `attachment`/서버판정 타입 |
| V-17 | apps/web CSP가 Report-Only(+unsafe-inline/eval) | 🟠MED | ⬜미결 | 강제 전환 |
| V-18 | 패스워드 만료정책 `PASS_MAX_DAYS=99999` | 🟠MED | ⬜미결 | `90` (login.defs) |
| V-19 | `/etc/issue` OS 버전 노출 | 🟢LOW | ⬜미결 | 배너 정보 제거 |
| V-20 | 의존성 CVE 스캔 미실시 | 🟠MED | 🔧진행(2026-06-20) | 스캔 실시: ERP_OT 61건(critical5·high24)·ot-brain node 1 low. next 14.2.35 패치완료(PR#76). fast-jwt critical=설정방어로 실영향無(보류). 남음: 기타 high·Next15·python pip-audit |
| V-21 | 네트워크 서브넷(C클래스) 분리 | 🟠MED | ⬜미결 | 스위치/VLAN — 네트워크 담당 협의 |
| V-22 | 스크립트 내 dev 계정(`dev1234`)·`INIT_PASSWORD` 평문 | 🟢LOW | ⬜미결 | env화 |

---

## 3. 양호 항목 (검증됨, 조치 불요)

- **JWT 코어**: HS256(알고리즘 혼동 차단), 시크릿 32자+ Zod 검증·폴백 없음, Access 1h/Refresh 7d, 리프레시 sha256 저장+회전+재사용탐지, bcrypt 12.
- **SQL Injection 없음**: 전부 Prisma 태그드 템플릿/`$N` 바인딩(knowledge-api 동적 SQL도 토큰은 바인딩, hex 가드).
- **게시판 XSS 없음**: react-markdown + `rehypeSanitize`, `rehype-raw` 미사용, `dangerouslySetInnerHTML` 없음.
- **경로탐색 차단**: `path.basename`+정규화+`..`/절대경로 거부, 저장명은 UUID.
- **업로드**: 크기/타입 allowlist 적용.
- **커맨드 인젝션·SSRF 없음**: child_process 없음, fetch는 env 내부 URL만.
- **redis 인증 있음**(`--requirepass`), `.env` 미커밋(.gitignore), Helmet/HSTS/보안헤더 적용.
- **KISA-U 리눅스**: UID0=root만, /etc/passwd(644)·shadow(640), telnet/ftp/r-서비스 없음, world-writable 없음, PermitEmptyPasswords no.

---

## 4. 진행 기록 (Journal)

### 2026-06-13 (1차 종합 보안 점검)
- 병렬 코드 리뷰(인증/인가·주입/XSS·시크릿/설정) + 인프라 실측 + KISA-U 리눅스/SSH 점검 수행.
- **즉시 패치·검증·푸시**: V-01(/dev/token 제거), V-02(하드코딩 비번 env화), V-03(포트 127.0.0.1), V-04(ocr CORS). commits `2b4338b`, `767de16`.
- 발견: NODE_ENV=development 운영, SSH 비번인증 ON, IDOR 2건, 다수 MED. → 본 대장 V-05~V-22 등록.
- 외부 노출면을 SSH·web 2개로 축소 확인.
- **다음 작업(우선순위)**: ① SSH 키 인증 전환 + ufw(SSH 개발IP·web VPN대역) ② HTTPS 리버스프록시 → NODE_ENV=production ③ IDOR 코드 수정 ④ 의존성 CVE 스캔 ⑤ ecount DB 비번 교체.

### 2026-06-20 (IDOR 조치 — 배치 B ③)
- **V-10 approval IDOR 조치완료(PR#71)**: 결재문서 GET/:id·PUT/:id·PATCH/:id/submit에 권한검증 추가. `document.service.canAccess`(조회=ADMIN|기안자|결재자|참조)·`isOwner`(수정/상신=ADMIN|기안자), 위반 403. approve/reject/agree는 기존 step.approverId 검증 유지. tsc·FSM 11/11 통과.
- **V-11 project IDOR 정책수용**: 조회(GET/:id·gantt)는 전 직원 공개로 결정(사내 협업도구, approval 같은 개인 민감정보 아님). 수정/삭제는 기존 requireRole(ADMIN,MANAGER) 보호 → 코드변경 없음. 멤버검증(project→tasks→segments→assignments 다단계 join)은 성능비용 대비 실익 낮음.
- **남은 배치 B**: ① SSH 키인증+ufw ② HTTPS→NODE_ENV=production ④ 의존성 CVE 스캔 ⑤ ecount DB비번 교체 / + ot-brain 자산 approve·merge·delete 역할게이트, SVG업로드 제거, 다운로드 attachment 강제, CSP 강제.

### 2026-06-20 (자산 역할게이트 — 배치 B)
- **V-14 조치완료(ot-brain PR#13)**: knowledge-api 자산 approve/merge/confirm이 로그인한 모든 ERP 유저에게 열려 있던 것을 ADMIN/MANAGER·관리토큰 한정으로 게이트. preHandler가 ERP `/auth/me`의 role을 req.userRole에 보존(authCache {exp,role}), 관리토큰=ADMIN 매핑. 검증: 무효토큰 401·관리토큰 통과.
- **남은 배치 B(코드)**: SVG업로드 제거(저장형XSS)·다운로드 attachment 강제·CSP 강제·approval 커스텀 internalToken→공유 require-internal·의존성 CVE 스캔(npm/pip audit). **인프라**: HTTPS→NODE_ENV=production·SSH키+ufw. **외부**: ecount DB비번 교체.

### 2026-06-20 (의존성 CVE 스캔 — 배치 B ④)
- **스캔 실시(V-20 진행)**: ERP_OT `pnpm audit` 61건(critical 5·high 24·moderate 25·low 7) / ot-brain knowledge-api(node) 1 low(양호) / python(ingestion) pip-audit 미설치·내부배치라 후순위.
- **next 14.2.3→14.2.35 패치완료(PR#76)**: critical "미들웨어 Authorization Bypass"(patched≥14.2.25) + 14.2 라인 high/moderate 전부 해결. tsc 0·CI green. 잔여 14건은 Next 15 전용 → major 마이그레이션 분리.
- **fast-jwt critical 3건 재평가 → 실영향 없음(보류)**: empty HMAC bypass(JWT_ACCESS_SECRET.min(16)=빈secret아님)·alg confusion(HS256 명시)·cache confusion(register cache 옵션無=off) 모두 우리 설정으로 공격표면 차단. 패치(fast-jwt 6.2.4)=@fastify/jwt 10=**fastify 5 마이그레이션**(전 7서비스, 큰작업)이라 fastify5 시 동반. crit header advisory는 upstream 패치 없음(<0.0.0).
- **남은 CVE**: ERP_OT 기타 high(fastify/fast-uri path traversal, xlsx prototype pollution[npm패치無·sheetjs], socket.io DoS, tmp[exceljs]) + dev-only(vitest/ws/vite/picomatch=런타임무관) + Next15 라인 + python pip-audit. → 버전업+검증 별건.

<!-- 새 항목은 위 형식으로 이 아래에 날짜별 추가 -->

---

## 5. 운영 보안 수칙 (요약)
- 외부로 포트 개방 금지 — 신규 서비스는 기본 `127.0.0.1` 바인딩, 외부 필요 시 리버스프록시 경유.
- 비밀은 `.env`/시크릿 매니저만, 코드·스크립트 하드코딩 금지(env화).
- 운영 접근은 VPN + 방화벽(SSH 개발IP·서비스 내부대역)으로 제한.
- 변경 시 본 대장 갱신.
