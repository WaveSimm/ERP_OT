# 마일스톤-시점태스크-회귀 Design

> **PDCA Phase**: Design
> **Status**: Draft
> **Created**: 2026-05-01
> **Plan**: `docs/01-plan/features/마일스톤-시점태스크-회귀.plan.md`
> **Reverse 대상**: `프로젝트-마일스톤-재설계` (Match 93%, pdcaHistory)

---

## 1. Architecture Overview

### 1.1 Reverse 후 모델

```
┌─────────────────────────────────────────────────────────────┐
│                    Project                                   │
│      ┌────────────────────────────────────────────┐         │
│      │  Task (parentId WBS hierarchy)              │         │
│      │  ├─ isMilestone: false (일반 작업)          │         │
│      │  │   └─ TaskSegment (진척률 + 자원)          │         │
│      │  └─ isMilestone: true (시점 ◆)              │         │
│      │      └─ TaskSegment (start=end, 단일)        │         │
│      └────────────────────────────────────────────┘         │
│                                                              │
│      Dependency (Task ↔ Task만)                              │
└─────────────────────────────────────────────────────────────┘

❌ 폐기:
  - Milestone 모델
  - linkedTasks (m:n)
  - achievementCriteria, MilestoneStatus enum
  - Dependency.predecessorMilestoneId / successorMilestoneId
  - MilestoneService, milestone.routes
  - MilestoneModal, MilestoneSidePanel
```

### 1.2 시점 task 정의

isMilestone=true Task의 제약:
- segment 1개만 허용 (단일 시점)
- segment.startDate = segment.endDate 강제
- isManualProgress=true (PM 수동 진척)
- 자원 배정 가능 (Planner 호환)
- 자식 task 가질 수 없음 (parentId의 역방향 — 자기 parentId만 있음)

### 1.3 시각화

| Task 종류 | 갠트 표시 |
|----------|----------|
| 일반 (isMilestone=false) | 막대 (effectiveStart~End), parent 시 ▼ 토글 |
| 시점 (isMilestone=true) | ◆ 다이아몬드 (status 색상 4단계) |

색상 매핑:
- TODO → 회색
- IN_PROGRESS → 노랑
- DONE → 초록
- BLOCKED → 빨강

---

## 2. Schema Diff

### 2.1 Prisma schema 변경

```prisma
// services/project/prisma/schema.prisma

model Task {
  id              String     @id @default(cuid())
  projectId       String
  parentId        String?
  name            String
  description     String?
  status          TaskStatus @default(TODO)
  sortOrder       Int        @default(0)
  overallProgress Float      @default(0)
  isManualProgress Boolean   @default(false)
+ isMilestone     Boolean    @default(false)  // 부활: 시점 task (◆)
  isCritical      Boolean    @default(false)
  totalFloat      Float?
  createdBy       String
  createdAt       DateTime   @default(now())
  updatedAt       DateTime   @updatedAt
  effectiveStartDate DateTime? @map("effective_start_date")
  effectiveEndDate   DateTime? @map("effective_end_date")

  project           Project   @relation(...)
  parent            Task?     @relation("TaskHierarchy", ...)
  children          Task[]    @relation("TaskHierarchy")
  segments          TaskSegment[]
  comments          Comment[]
  attachments       Attachment[]
  scheduleHistories TaskScheduleHistory[]
  taskBaselines     TaskBaseline[]
  workLogs          WorkLog[]

- linkedMilestones  Milestone[]  @relation("MilestoneLinkedTasks")  // 폐기

  predecessorOf     Dependency[] @relation("PredecessorTask")
  successorOf       Dependency[] @relation("SuccessorTask")

  @@map("tasks")
  @@schema("project")
}

- model Milestone { ... }                    // 폐기
- enum MilestoneStatus { ... }                // 폐기
- enum AchievementCriteria { ... }            // 폐기

model Dependency {
  id                       String          @id @default(cuid())
- predecessorTaskId        String?
+ predecessorTaskId        String           // NOT NULL
- predecessorMilestoneId   String?          // 폐기
- successorTaskId          String?
+ successorTaskId          String           // NOT NULL
- successorMilestoneId     String?          // 폐기
  dependencyType           DependencyType  @default(FS)
  lag                      Int             @default(0)
  createdBy                String
  createdAt                DateTime        @default(now())

  predecessorTask  Task @relation("PredecessorTask", fields: [predecessorTaskId], references: [id], onDelete: Cascade)
  successorTask    Task @relation("SuccessorTask", fields: [successorTaskId], references: [id], onDelete: Cascade)

- predecessorMilestone Milestone? @relation(...)  // 폐기
- successorMilestone   Milestone? @relation(...)  // 폐기

  @@index([predecessorTaskId])
  @@index([successorTaskId])
  @@unique([predecessorTaskId, successorTaskId])  // 신규
  @@map("dependencies")
  @@schema("project")
}
```

---

## 3. Migration SQL

### 3.1 파일

`services/project/prisma/migrations/20260501120000_milestone_revert/migration.sql`

### 3.2 단일 트랜잭션

```sql
BEGIN;

-- ═══════════════════════════════════════════════════
-- 1. Task에 isMilestone 컬럼 부활
-- ═══════════════════════════════════════════════════
ALTER TABLE project.tasks
  ADD COLUMN "isMilestone" BOOLEAN NOT NULL DEFAULT false;

-- ═══════════════════════════════════════════════════
-- 2. Milestone → Task 변환
-- ═══════════════════════════════════════════════════
INSERT INTO project.tasks (
  id, "projectId", "parentId", name, description, "sortOrder",
  status, "overallProgress", "isManualProgress", "isMilestone",
  "isCritical", "createdBy", "createdAt", "updatedAt"
)
SELECT
  'tsk_ms_' || substring(id from 5),  -- 'msl_xxx' → 'tsk_ms_xxx'
  "projectId",
  "displayParentId",
  name,
  description,
  "sortOrder",
  CASE WHEN status = 'ACHIEVED' THEN 'DONE'::project."TaskStatus" ELSE 'TODO'::project."TaskStatus" END,
  CASE WHEN status = 'ACHIEVED' THEN 100 ELSE 0 END,
  true,                  -- isManualProgress
  true,                  -- isMilestone (시점)
  false,                 -- isCritical (CPM 미적용)
  "createdBy",
  "createdAt",
  "updatedAt"
FROM project.milestones;

-- ═══════════════════════════════════════════════════
-- 3. 변환된 task에 단일 segment (start=end=dueDate)
-- ═══════════════════════════════════════════════════
INSERT INTO project.task_segments (
  id, "taskId", name, "sortOrder", "startDate", "endDate",
  "progressPercent", "createdAt", "updatedAt"
)
SELECT
  'seg_ms_' || substring(m.id from 5),
  'tsk_ms_' || substring(m.id from 5),
  '시점',
  0,
  COALESCE(m."dueDate", CURRENT_DATE),
  COALESCE(m."dueDate", CURRENT_DATE),  -- 단일 시점
  CASE WHEN m.status = 'ACHIEVED' THEN 100 ELSE 0 END,
  NOW(), NOW()
FROM project.milestones m;

-- ═══════════════════════════════════════════════════
-- 4. ACHIEVED milestone에 system work log
-- ═══════════════════════════════════════════════════
INSERT INTO project.work_logs (
  id, task_id, author_id, author_name, content, worked_at,
  is_deleted, created_at, updated_at
)
SELECT
  'wl_ms_' || substring(m.id from 5),
  'tsk_ms_' || substring(m.id from 5),
  'system',
  'system',
  '[system] 마일스톤-시점태스크-회귀 마이그레이션. 이전 ACHIEVED milestone 변환.',
  COALESCE(m."achievedDate"::date, m."dueDate", CURRENT_DATE),
  false,
  NOW(), NOW()
FROM project.milestones m
WHERE m.status = 'ACHIEVED';

-- ═══════════════════════════════════════════════════
-- 5. Dependency의 milestone 참조 정리 (현재 0건이지만 안전)
-- ═══════════════════════════════════════════════════
DELETE FROM project.dependencies
WHERE "predecessorMilestoneId" IS NOT NULL
   OR "successorMilestoneId" IS NOT NULL;

ALTER TABLE project.dependencies
  DROP CONSTRAINT IF EXISTS dep_predecessor_xor,
  DROP CONSTRAINT IF EXISTS dep_successor_xor,
  DROP CONSTRAINT IF EXISTS dep_no_self_milestone;

ALTER TABLE project.dependencies
  DROP COLUMN "predecessorMilestoneId",
  DROP COLUMN "successorMilestoneId";

ALTER TABLE project.dependencies
  ALTER COLUMN "predecessorTaskId" SET NOT NULL,
  ALTER COLUMN "successorTaskId" SET NOT NULL;

-- 기존 dep_no_self_task CHECK은 그대로 유지 (자기 자신 의존 금지)
ALTER TABLE project.dependencies
  ADD CONSTRAINT IF NOT EXISTS dep_no_self_task
  CHECK ("predecessorTaskId" <> "successorTaskId");

-- 신규: predecessor + successor 유니크 (중복 의존성 방지)
ALTER TABLE project.dependencies
  ADD CONSTRAINT dependencies_predecessor_successor_key
  UNIQUE ("predecessorTaskId", "successorTaskId");

-- ═══════════════════════════════════════════════════
-- 6. 백필 검증 (RAISE EXCEPTION on mismatch → 자동 ROLLBACK)
-- ═══════════════════════════════════════════════════
DO $$
DECLARE
  expected_ms INT;
  actual_tasks INT;
  actual_segs INT;
  expected_wl INT;
  actual_wl INT;
BEGIN
  SELECT COUNT(*) INTO expected_ms FROM project.milestones;
  SELECT COUNT(*) INTO actual_tasks FROM project.tasks WHERE id LIKE 'tsk_ms_%';
  SELECT COUNT(*) INTO actual_segs FROM project.task_segments WHERE id LIKE 'seg_ms_%';
  SELECT COUNT(*) INTO expected_wl FROM project.milestones WHERE status = 'ACHIEVED';
  SELECT COUNT(*) INTO actual_wl FROM project.work_logs WHERE id LIKE 'wl_ms_%';

  IF expected_ms != actual_tasks THEN
    RAISE EXCEPTION 'Task 변환 mismatch: ms=%, tasks=%', expected_ms, actual_tasks;
  END IF;
  IF expected_ms != actual_segs THEN
    RAISE EXCEPTION 'Segment 생성 mismatch: ms=%, segs=%', expected_ms, actual_segs;
  END IF;
  IF expected_wl != actual_wl THEN
    RAISE EXCEPTION 'Work log 생성 mismatch: expected=%, actual=%', expected_wl, actual_wl;
  END IF;

  RAISE NOTICE 'Migration verified: % milestones → % tasks + % segments + % work logs',
               expected_ms, actual_tasks, actual_segs, actual_wl;
END $$;

-- ═══════════════════════════════════════════════════
-- 7. 폐기
-- ═══════════════════════════════════════════════════
DROP TABLE project."_MilestoneLinkedTasks";
DROP TABLE project.milestones;
DROP TYPE project."MilestoneStatus";
DROP TYPE project."AchievementCriteria";

COMMIT;
```

### 3.3 Rollback

문제 발생 시:
```bash
docker exec erp-ot-postgres psql -U erp_user -d erp_ot \
  < backup-pre-milestone-revert-{TIMESTAMP}.sql
```

코드도 git revert.

---

## 4. Backend 변경

### 4.1 폐기 파일 (DROP)

| 파일 | 사유 |
|------|------|
| `services/project/src/application/milestone.service.ts` | 모델 폐기 |
| `services/project/src/api/routes/milestone.routes.ts` | 라우트 폐기 |

### 4.2 수정 파일

#### 4.2.1 `services/project/src/index.ts`
```diff
- import { MilestoneService } from "./application/milestone.service.js";
- import { milestoneRoutes } from "./api/routes/milestone.routes.js";
- const milestoneService = new MilestoneService(prisma, gateway);
- taskService.setMilestoneService(milestoneService);
- app.decorate("milestoneService", milestoneService);
- app.register(milestoneRoutes, { prefix: "/api/v1" });

  // dependency setter는 유지 (CPM 트리거 위해)
  dependencyService.setCpmService(cpmService);
```

#### 4.2.2 `services/project/src/application/dependency.service.ts`
- `NodeType = "TASK" | "MILESTONE"` → 단순 string `taskId`만
- `predecessorMilestoneId/successorMilestoneId` 분기 모두 제거
- BFS cycle detection — task만 traversal
- 4-cases polymorphic 로직 제거

#### 4.2.3 `services/project/src/api/routes/dependency.routes.ts`
- DTO 단순화: `predecessorTaskId, successorTaskId, dependencyType, lag` 만
- `predecessorType/successorType` enum 폐기
- `cycle` 검증 그대로 (task만)

#### 4.2.4 `services/project/src/application/cpm.service.ts`
- 의존성 조회 쿼리 단순화 (`predecessorTaskId NOT NULL` 조건 불필요 — 항상 NOT NULL)
- 코드 약간 정리

#### 4.2.5 `services/project/src/application/aggregate.service.ts`
- `milestoneService` 의존 주입 제거
- `onTaskUpdated()` 의 `milestoneService.onTaskProgressChange(taskId)` 호출 제거

#### 4.2.6 `services/project/src/application/dashboard/issue-detector.service.ts`
- 마일스톤 status 감지 (`AT_RISK`, `MISSED`, `INFEASIBLE` 등) 코드 제거
- D-3 임박 알림: 시점 task (isMilestone=true) 직접 task 테이블에서 조회

#### 4.2.7 `services/project/src/application/dashboard/dashboard.service.ts`
- 이번 주 milestones count: `task.isMilestone=true` + dueDate 범위 필터로 변경

#### 4.2.8 `services/project/src/application/dashboard/timeline.service.ts`
- milestone separate query 폐기 → segment query에서 task.isMilestone 분기로 통합

#### 4.2.9 `services/project/src/application/task.service.ts`
- `setMilestoneService`, `notifyMilestoneOfTaskChange` 제거
- `createTask`/`updateTask`: isMilestone 필드 받기
- `createSegment`: 부모 task가 isMilestone=true면 segment.startDate=endDate 강제
  - 또는 segment 1개만 허용 (이미 있으면 거부)

```typescript
async createSegment(taskId: string, dto: CreateSegmentDto, requesterId: string): Promise<TaskSegment> {
  const task = await this.prisma.task.findUnique({...});
  if (!task) throw ...;

  // 시점 task 검증
  if (task.isMilestone) {
    if (task.segments.length > 0) {
      throw new AppError(409, "MILESTONE_SINGLE_SEGMENT", "시점 task는 segment 1개만 허용됩니다.");
    }
    if (dto.startDate !== dto.endDate) {
      // 자동 보정 또는 에러
      dto.endDate = dto.startDate;  // 보정
    }
  }
  // 기존 로직 ...
}
```

#### 4.2.10 `services/project/src/api/dtos/task.dto.ts`
- `isMilestone?: boolean` 부활

#### 4.2.11 `services/project/src/api/routes/task.routes.ts`
- Zod schema에 `isMilestone: z.boolean().optional()` 부활

#### 4.2.12 `services/project/src/application/project.service.ts`
- `getGanttData`에서 `milestones` 별도 키 반환 로직 제거
- 응답 shape: `{ tasks, dependencies, criticalPath }` (이전 단순 형태)
- `cloneProject`/`instantiateFromTemplate`의 milestone 처리 제거 (또는 isMilestone task 복제)

### 4.3 신규 검증 (TaskService)

```typescript
// task.service.ts
private validateMilestoneSegment(task: Task, dto: CreateSegmentDto): void {
  if (!task.isMilestone) return;
  if (task.segments?.length > 0) {
    throw new AppError(409, "MILESTONE_SINGLE_SEGMENT",
      "시점 task는 segment 1개만 허용됩니다.");
  }
  // start=end 자동 보정
  if (dto.startDate !== dto.endDate) {
    dto.endDate = dto.startDate;
  }
}
```

---

## 5. Frontend 변경

### 5.1 폐기 파일

| 파일 | 처리 |
|------|------|
| `apps/web/src/components/MilestoneModal.tsx` | DELETE |
| `apps/web/src/components/MilestoneSidePanel.tsx` | DELETE |

### 5.2 수정 파일

#### 5.2.1 `apps/web/src/lib/api.ts`
- `milestoneApi` 객체 전체 폐기
- `dependencyApi`: type 단순화 (predecessorTaskId, successorTaskId만, polymorphic 제거)

#### 5.2.2 `apps/web/src/components/AddTaskModal.tsx`
- `isMilestone` toggle 부활 (이전 패턴 — 보라색 ◆)
- 토글 시: 단일 날짜 입력 (기간 입력 → 시점 입력)
- 토글 시: 자원 배정 섹션 숨김 (선택 사항 — 시점은 자원 의미 약함)

```tsx
const [isMilestone, setIsMilestone] = useState(false);
const [milestoneDate, setMilestoneDate] = useState(...);

// 토글 UI 부활
<label>
  <input type="checkbox" checked={isMilestone} onChange={...} />
  <span>◆ 시점 마일스톤</span>
</label>

// 날짜 입력 분기
{isMilestone ? (
  <input type="date" value={milestoneDate} ... />
) : (
  <>
    <input type="date" value={startDate} ... />
    <input type="date" value={endDate} ... />
  </>
)}

// 제출 시
const taskData = { name, isMilestone, ... };
const segmentData = isMilestone
  ? { name: "시점", startDate: milestoneDate, endDate: milestoneDate }
  : { name: segName, startDate, endDate };
```

#### 5.2.3 `apps/web/src/components/GanttChart.tsx`
- `mergeGanttData` 호출 흐름 폐기 (project page에서 호출 안 함)
- Task 타입에 `isMilestone` 부활 (이전 처럼)
- ◆ 렌더링은 이미 task.isMilestone 분기로 되어 있음 — milestoneStatus 대신 task.status 기준 색상으로 변경

```tsx
// status별 색상 매핑 (milestoneStatus 대신 task.status)
const statusColor = task.status === "DONE" ? "bg-emerald-500"
  : task.status === "IN_PROGRESS" ? "bg-amber-500"
  : task.status === "BLOCKED" ? "bg-red-500"
  : "bg-gray-400";  // TODO
```

- 의존선 polymorphic 처리 제거 (Task↔Task만)

#### 5.2.4 `apps/web/src/app/projects/[id]/page.tsx`
- `mergeGanttData()` 함수 자체 삭제
- MilestoneModal/SidePanel import 제거
- showMilestoneModal·editingMilestoneId·milestoneRefreshKey state 제거
- ◆ 클릭 → MilestoneModal 분기 제거 (그냥 TaskDrawer로 가도록)
- 갠트 우측 SidePanel 자체 삭제 (또는 유지하되 task list로)

---

## 6. Import 스크립트 변경

### 6.1 `scripts/import-planner.py`

```python
# Pass 2 — Task 생성에서 isMilestone 처리 통합
for row in raw_tasks:
    task_num = row.get("작업 번호")
    overview = str(row.get("개요 번호") or "").strip()
    is_milestone_excel = (row.get("마일스톤") == "예")

    sort_order = task_num if isinstance(task_num, int) else 0
    body = {
        "name": str(row.get("이름") or "(이름없음)"),
        "sortOrder": sort_order,
        "isMilestone": is_milestone_excel,  # ★ 부활
    }
    t = post(f"/projects/{project_id}/tasks", token, body)
    overview_to_task_id[overview] = t["id"]
    task_number_to_task_id[task_num] = t["id"]
    task_records.append({
        "row": row,
        "task_id": t["id"],
        "is_milestone": is_milestone_excel,  # WorkLog 등 후속에서 활용
        "overview": overview,
        "task_num": task_num,
    })

# Pass 4 — Segment 생성: 시점 task는 start=end
for rec in task_records:
    row = rec["row"]
    start = to_iso_date(row.get("시작"))
    end = to_iso_date(row.get("마침"))
    if not start: continue
    if rec["is_milestone"]:
        end = start  # 시점 task는 단일 일자
    elif not end or end < start:
        end = start
    seg = post(f"/projects/{project_id}/tasks/{rec['task_id']}/segments", token, {
        "name": "시점" if rec["is_milestone"] else str(row.get("이름") or "구간"),
        "startDate": start,
        "endDate": end,
    })
    rec["segment_id"] = seg["id"]

# Pass 6 (Milestone) — 폐기 (Task로 통합됨)

# Pass 9 (Progress update) — 시점 task도 동일 (Excel %=100이면 100%)
```

### 6.2 `scripts/fix-planner-sortorder.py`

```python
# milestone 보정 로직 폐기 (마일스톤이 task가 됨)
# task name → sortOrder 매핑만 유지 (시점·일반 모두 task)
def fix(project_id, excel_path, token):
    rows = parse(excel_path)
    name_to_sort = {}
    for row in rows:
        name = str(row.get("이름") or "").strip()
        num = row.get("작업 번호")
        if name and isinstance(num, int):
            name_to_sort[name] = num   # 시점·일반 구분 없이

    # task patch만, milestone PATCH 호출 모두 제거
```

---

## 7. 플레이북 갱신

### 7.1 `docs/04-operation/migration-playbook-planner-to-erp.md`

- §1 D8 마일스톤 매핑 정책 갱신:
  ```
  D8 마일스톤 (Q="예") → Task isMilestone=true + 단일 segment(start=end=마침일)
  (이전: Milestone 모델로 변환 — reverse됨)
  ```
- §2 처리 흐름 (9 Pass) → Pass 6(Milestone) 폐기, 8 Pass로 단순화
- §5에 새 이슈 없음 (이미 알려진 마일스톤 모델 의미 모호 — reverse가 답)
- §8 변경 이력에 추가

### 7.2 `docs/04-operation/migration-playbook-마일스톤-재설계.md`

- §1 또는 §10에 reverse 명시:
  ```
  ⚠ 본 PDCA의 일부 결정은 후속 PDCA "마일스톤-시점태스크-회귀"에서 reverse됨
  - (D) Milestone 분리, (b) 자동 추적, (4c) polymorphic dep 등
  - 사유: 사용자 멘탈 모델 ("시점 확인")과 모델 의미 분리 불일치
  - 데이터 영향: KHOA 1건 → Task 변환
  ```

---

## 8. Implementation Order

```
Step 1. Schema 수정 (Task isMilestone 부활, Milestone/enum/m:n 폐기, Dependency 단순화)
Step 2. Migration SQL 작성 + 백필 검증 embedded
Step 3. Prisma format/validate
Step 4. Backend 정리 (10 파일 수정 + 2 파일 폐기)
        - milestone.service.ts, milestone.routes.ts DELETE
        - dependency.service.ts, dependency.routes.ts 단순화
        - cpm.service.ts, aggregate.service.ts, issue-detector.service.ts 정리
        - task.service.ts (isMilestone 검증 추가), task.routes.ts (Zod), task.dto.ts
        - project.service.ts (gantt response 단순화)
        - index.ts wiring 정리
Step 5. TypeScript 백엔드 컴파일
Step 6. Frontend 정리 (4 파일 수정 + 2 파일 폐기)
        - MilestoneModal.tsx, MilestoneSidePanel.tsx DELETE
        - lib/api.ts (milestoneApi 폐기, dependencyApi 단순화)
        - AddTaskModal.tsx (isMilestone 토글 부활)
        - GanttChart.tsx (status 색상 매핑, polymorphic 제거)
        - app/projects/[id]/page.tsx (mergeGanttData/SidePanel 제거)
Step 7. TypeScript 프런트엔드 컴파일
Step 8. Import 스크립트 + Playbook 갱신
Step 9. Migration 적용 (개발 DB) + 백엔드 재기동
Step 10. KHOA 검증 (curl + 시각)
        - "현장 납품" task ◆로 표시
        - status=DONE, sortOrder 31, parentId=납품
        - dueDate=2026-04-15
        - WorkLog 1건 (system 자동 생성)
        - Dependency 12건 안전
Step 11. Zero Script QA 5 시나리오
Step 12. gap-detector 호출 → Match Rate
```

---

## 9. Test Scenarios — Zero Script QA

| QA | 시나리오 | 검증 |
|----|---------|------|
| QA-1 | AddTaskModal에서 isMilestone=true + 단일 날짜로 task 생성 | task.isMilestone=true, segment 1건 (start=end) |
| QA-2 | isMilestone=true task에 segment 추가 (이미 1개 있음) | 409 MILESTONE_SINGLE_SEGMENT 거부 |
| QA-3 | KHOA 마이그레이션 후 "현장 납품" 갠트 표시 | ◆ + dueDate 4/15 + status DONE 색상 (초록) |
| QA-4 | TaskDrawer에서 시점 task의 segment 일정 변경 | start=end만 받음, 다른 입력 시 자동 보정 또는 거부 |
| QA-5 | Dependency 12건 모두 정상 표시 (폴리morphic 제거 후) | 갠트에 12개 화살표, FF 1건도 정상 |

추가 회귀:
- 프로젝트-관리: project create/update, task CRUD, gantt 표시
- 프로젝트-진도율-캐시: leaf task 진척률 (시점 task도 leaf로 처리되는지)

---

## 10. Decisions (Open Questions Resolved)

| # | 질문 | 결정 |
|---|------|------|
| **OQ-1** | 시점 task의 자원 배정 허용? | **허용** — Planner 호환, "확인 책임자" 의미. AddTaskModal 자원 섹션 노출 |
| **OQ-2** | 시점 task가 자식 task를 가질 수 있나? | **금지** — TaskService 검증: task의 parentId가 isMilestone=true인 task면 `409 MILESTONE_CANNOT_HAVE_CHILDREN` |
| **OQ-3** | ACHIEVED 변환 시 system work log? | **자동 생성 + achievedDate 보존** — `worked_at = COALESCE(achievedDate::date, dueDate, NOW)`. content에 마이그레이션 사유 기록 |
| **OQ-4** | `mergeGanttData()` 완전 삭제 vs 빈 stub? | **완전 삭제** — 호출처 4곳 직접 `await projectApi.gantt(projectId)` 사용. dead code 정리 |
| **OQ-5** | 갠트 우측 SidePanel 폐기 vs 다른 용도? | **완전 폐기** — 갠트 가로 공간 확보. 새 사이드 패널 필요 시 별도 PDCA |
| **OQ-6** | CPM 시점 task 처리 (duration)? | **duration=0 (PMBOK 표준)** ⭐ — `cpm.service.ts`에서 isMilestone task duration 강제 0 (코드 1줄). 후행 task 1일 push 방지 |
| **OQ-7** | 이전 마일스톤-재설계 마이그레이션 파일? | **보존** — Prisma 순차 이력. 새 reverse 마이그레이션이 forward 적용 |

### OQ-6 구체 (PMBOK 표준 적용)

```ts
// services/project/src/application/cpm.service.ts:49 부근
const cpmInputs = tasks.map((task) => {
  if (task.isMilestone) return { taskId: task.id, duration: 0 };  // ★ PMBOK 표준
  if (task.segments.length === 0) return { taskId: task.id, duration: 1 };
  // 기존 로직 ...
});
```

추가 영향:
- `aggregate.service.ts`: 시점 task도 leaf 진척률 계산에 포함되나, segment 1개의 progressPercent만 반영 → 영향 미미
- `GanttChart.tsx`: 시점 task의 "기간" 컬럼은 ◆로 표시되어 막대 안 그려짐 — 별도 처리 불필요

---

## 11. Risks & Mitigations

| Risk | 영향 | 완화 |
|------|:---:|------|
| KHOA Milestone 1건 변환 실패 | 高 | 백필 검증 embedded SQL → 자동 ROLLBACK |
| Dependency 12건 회귀 | 中 | 모두 task↔task만이라 안전. Migration §5에서 milestone 참조 0건 확인 |
| 다른 PDCA 영향 (프로젝트-관리, 진도율-캐시) | 中 | aggregate.service의 leaf 계산 그대로 (시점 task도 leaf) |
| 프런트 컴포넌트 폐기 후 import error | 低 | TypeScript가 즉시 잡음 |
| 운영 중 마일스톤 사용자 혼란 | 低 | KHOA 1건만 — PM에게 사전 안내 |
| 마일스톤-재설계 PDCA 노력 무효 | 中 | "사용 후 단순화" 정직 표현, 재시도 가치 인정 |

---

## 12. Next Steps

1. **즉시**: Design 검토 → `/pdca do 마일스톤-시점태스크-회귀`
2. **Do 단계 진입 전**: OQ-1~OQ-7 결정
3. **Do 단계**: Step 1~12 순서. 단일 PR (백엔드+프런트+마이그레이션). 코드 변경량 ~15 파일.
4. **Check**: gap-detector → Match Rate ≥ 90% 목표
5. **Report**: 마일스톤-재설계 reverse 사유 명시
6. **후속 PDCA 후보**:
   - 누적 Report 정리 (마일스톤-재설계, prod-빌드-정리)
   - `monorepo-dockerfile-정리` (Plan 단계 그대로)
   - `milestone-responsible` (별도 PDCA, 시점 task 책임자 컬럼) — 운영 후 PM 피드백
