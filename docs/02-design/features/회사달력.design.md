# [Design] 회사달력 (Company Calendar)

> **Feature**: 회사달력
> **Phase**: Design
> **Created**: 2026-04-27
> **Updated**: 2026-04-27
> **Status**: Design v1.0
> **Planning Doc**: [회사달력.plan.md](../../01-plan/features/회사달력.plan.md)
> **Author**: AI + Team

---

## 1. 개요

### 1.1 설계 목표

1. **단일 모델**로 4 타입 (PUBLIC_HOLIDAY/COMPANY_HOLIDAY/EVENT/WORKDAY) 통합 관리
2. **2차 확장 컬럼 미리 추가** — recurrence/targetDepartmentId/startTime/endTime은 더미로 두고 향후 활성화
3. **자체 캘린더 UI** — 외부 라이브러리(FullCalendar 등) 의존 0, Tailwind grid 7×6
4. **read-only 소비** — attendance/project가 internal HTTP로 조회 (향후)
5. **권한 가드** — 모든 변경 API에 ADMIN 가드, 조회는 인증자 전체 허용

### 1.2 설계 원칙

- **Single Model**: CompanyCalendarEntry 1개 테이블만 추가
- **Snapshot 없음**: 단순 등록 정보, 변경 시 즉시 반영
- **Soft Delete 미사용**: 1차 단순 hard delete (작은 데이터·관리 부담↓)
- **Forward-Compatible**: 2차 컬럼이 nullable → 마이그레이션 추가 비용 0

---

## 2. 아키텍처

### 2.1 컴포넌트 다이어그램

```
┌──────────────────────────────────────────────────────────────────┐
│                       apps/web (Next.js)                          │
│  ┌────────────────────────┐  ┌────────────────────────────────┐  │
│  │ /admin/calendar        │  │ Home/Dashboard (선택)           │  │
│  │  ├─ MonthCalendar      │  │  └─ NextHolidaysCard           │  │
│  │  ├─ EventList          │  │                                │  │
│  │  └─ EntryModal         │  │                                │  │
│  └──────────┬─────────────┘  └─────────┬──────────────────────┘  │
└─────────────┼─────────────────────────┼─────────────────────────┘
              ▼                         ▼
┌──────────────────────────────────────────────────────────────────┐
│           services/auth (port 3001) — REST API                    │
│  ┌────────────────────────┐  ┌────────────────────────┐          │
│  │ calendar.routes.ts     │  │ internal.routes (확장) │          │
│  │  /api/v1/calendar      │  │  /internal/calendar/.. │          │
│  └────────────┬───────────┘  └─────────┬──────────────┘          │
│               ▼                        ▼                          │
│  ┌────────────────────────────────────────────────────┐          │
│  │ calendar.service.ts (CRUD + range 조회)            │          │
│  └─────────────┬──────────────────────────────────────┘          │
│                ▼                                                  │
│            Prisma (PostgreSQL — public schema, auth-service)      │
└──────────────────────────────────────────────────────────────────┘
                                      ▲
                                      │ (2차)
                  ┌───────────────────┴────────────────────┐
                  │                                        │
       attendance-service                          project-service
       (working day 계산)                          (Gantt/CPM)
```

### 2.2 데이터 흐름 — 등록 시

```
ADMIN → POST /api/v1/calendar
  ├─ 1. authenticate hook + requireRole("ADMIN")
  ├─ 2. Zod 검증 (type, startDate ≤ endDate, 길이)
  ├─ 3. Prisma create
  └─ 4. 201 → 객체 반환
```

### 2.3 의존성

| 컴포넌트 | 의존 | 목적 |
|---|---|---|
| calendar.routes | calendar.service, requireRole | CRUD + 가드 |
| calendar.service | Prisma | 단순 CRUD |
| 관리 UI | calendarApi | 화면 |
| Home Card (선택) | calendarApi.upcoming | 다음 항목 |
| (2차) attendance/project | internal HTTP /internal/calendar/holidays | 휴일 조회 |

---

## 3. 데이터 모델

### 3.1 Prisma 스키마 (services/auth/prisma/schema.prisma 추가분)

```prisma
model CompanyCalendarEntry {
  id          String              @id @default(cuid())
  type        CalendarEntryType
  title       String
  description String?
  startDate   DateTime            @db.Date @map("start_date")
  endDate     DateTime            @db.Date @map("end_date")
  isAllDay    Boolean             @default(true) @map("is_all_day")
  color       String?                                  // hex 또는 null (타입별 기본)

  // 2차 활성화 컬럼 (nullable, 1차 미사용)
  recurrence         String?                            // "YEARLY" 등
  targetDepartmentId String?  @map("target_department_id")
  startTime          String?  @map("start_time")        // "HH:mm"
  endTime            String?  @map("end_time")

  createdBy   String              @map("created_by")
  createdAt   DateTime            @default(now()) @map("created_at")
  updatedAt   DateTime            @updatedAt @map("updated_at")

  @@index([startDate, endDate])    // 기간 조회 (홈·관리 UI 핫패스)
  @@index([type])                   // 타입 필터
  @@map("company_calendar_entries")
}

enum CalendarEntryType {
  PUBLIC_HOLIDAY
  COMPANY_HOLIDAY
  EVENT
  WORKDAY
}
```

### 3.2 핵심 인덱스

| 인덱스 | 용도 |
|---|---|
| `(startDate, endDate)` | 월간 조회 — `WHERE NOT (endDate < $from OR startDate > $to)` |
| `(type)` | 타입 필터 (휴일만, 행사만 등) |

### 3.3 검증 규칙 (Zod)

- `type ∈ enum 4종`
- `title.length 1~200`
- `description.length ≤ 1000`
- `startDate, endDate` YYYY-MM-DD 형식
- `endDate >= startDate` (refine)
- `color` `^#[0-9a-fA-F]{6}$` 또는 null

---

## 4. API 명세

### 4.1 엔드포인트 목록 (6개)

| Method | Path | 설명 | 권한 |
|---|---|---|---|
| GET | `/api/v1/calendar?from&to&type` | 기간·타입 필터 조회 | 인증자 |
| GET | `/api/v1/calendar/upcoming?days=N` | 오늘부터 N일 (홈 카드용) | 인증자 |
| GET | `/api/v1/calendar/:id` | 단건 조회 | 인증자 |
| POST | `/api/v1/calendar` | 등록 | ADMIN |
| PATCH | `/api/v1/calendar/:id` | 수정 | ADMIN |
| DELETE | `/api/v1/calendar/:id` | 삭제 | ADMIN |

### 4.2 Internal API (1개)

| Method | Path | 설명 | 인증 |
|---|---|---|---|
| GET | `/internal/calendar/holidays?from&to` | PUBLIC_HOLIDAY + COMPANY_HOLIDAY 만 | X-Internal-Token |

### 4.3 주요 엔드포인트 상세

#### `GET /api/v1/calendar`

**Query Params:**
| 파라미터 | 타입 | 기본 | 설명 |
|---|---|---|---|
| `from` | YYYY-MM-DD | 당월 1일 | 조회 시작 |
| `to` | YYYY-MM-DD | 당월 말일 | 조회 끝 |
| `type` | enum | — | 타입 필터 (옵션) |

**Response (200):**
```json
[
  {
    "id": "ckxx...",
    "type": "COMPANY_HOLIDAY",
    "title": "창립기념일",
    "description": "전사 휴무",
    "startDate": "2026-05-15",
    "endDate": "2026-05-15",
    "isAllDay": true,
    "color": "#fb923c",
    "recurrence": null,
    "targetDepartmentId": null,
    "startTime": null,
    "endTime": null,
    "createdBy": "user_id",
    "createdAt": "2026-04-27T...",
    "updatedAt": "2026-04-27T..."
  }
]
```

정렬: `startDate asc, id asc`

#### `POST /api/v1/calendar`

**Request:**
```json
{
  "type": "COMPANY_HOLIDAY",
  "title": "창립기념일",
  "description": "전사 휴무",
  "startDate": "2026-05-15",
  "endDate": "2026-05-15",
  "color": "#fb923c"
}
```

**Response (201):** Calendar entry 객체

**Errors:**
- `400 INVALID_INPUT` — Zod 위반
- `400 INVALID_DATE_RANGE` — endDate < startDate
- `403 FORBIDDEN` — ADMIN 아님

#### `GET /api/v1/calendar/upcoming?days=14`

**Response (200):** 오늘 ~ N일 후의 항목 (정렬: `startDate asc`)

홈 카드용 — 휴일·행사 다가오는 5건 정도 표시.

#### `GET /internal/calendar/holidays?from&to`

**Headers:** `X-Internal-Token: ...`

**Response (200):**
```json
[
  { "date": "2026-01-01", "title": "신정", "type": "PUBLIC_HOLIDAY" },
  { "date": "2026-05-15", "title": "창립기념일", "type": "COMPANY_HOLIDAY" }
]
```

기간 내 휴일을 일자별로 정규화(date range를 일별 분해)하여 반환 — attendance/project가 그대로 일자 비교 사용.

---

## 5. UI/UX 설계

### 5.1 라우트

```
/admin/calendar                  — 월간 캘린더 (관리자만)
```

관리자 메뉴 드롭다운에 "📅 회사 달력" 추가.

### 5.2 와이어프레임 — 월간 캘린더 (`/admin/calendar`)

```
┌─ AppLayout 헤더 ────────────────────────────────────────────────┐
├─────────────────────────────────────────────────────────────────┤
│ 📅 회사 달력                                                     │
│ [◀ 4월] 2026년 5월 [6월 ▶]              [+ 항목 추가]   타입필터: [전체▼] │
├─────────────┬──────────────────────────────────────────────────┤
│ 사이드바    │ 일  월  화  수  목  금  토                         │
│ 5월 항목    │ ┌──┬──┬──┬──┬──┬──┬──┐                            │
│              │ │  │  │  │  │ 1│ 2│ 3│  ← 1주차                   │
│ 🔴 5/5      │ ├──┼──┼──┼──┼──┼──┼──┤                            │
│ 어린이날    │ │ 4│ 5│ 6│ 7│ 8│ 9│10│  5/5 빨간색 점·라벨        │
│              │ │  │🔴│  │  │  │  │  │                            │
│ 🟠 5/15     │ ├──┼──┼──┼──┼──┼──┼──┤                            │
│ 창립기념일  │ │11│12│13│14│15│16│17│  5/15 주황 라벨            │
│              │ │  │  │  │  │🟠│  │  │                            │
│ 🔵 5/20-22  │ ├──┼──┼──┼──┼──┼──┼──┤                            │
│ 워크숍      │ │18│19│20│21│22│23│24│  5/20-22 파랑 막대         │
│              │ │  │  │🔵════════════│                            │
│              │ ├──┼──┼──┼──┼──┼──┼──┤                            │
│              │ │25│26│27│28│29│30│31│                            │
│              │ └──┴──┴──┴──┴──┴──┴──┘                            │
└─────────────┴──────────────────────────────────────────────────┘
```

### 5.3 와이어프레임 — 추가/수정 모달

```
┌─ 항목 추가 ───────────────────────────────────────┐
│ 타입: [공휴일 ▼]                                  │
│        ├ 🔴 공휴일                                │
│        ├ 🟠 자체 휴일                              │
│        ├ 🔵 회사 행사                              │
│        └ ⚪ 특별 근무일                            │
│                                                   │
│ 제목: [_____________________________]             │
│                                                   │
│ 시작일: [2026-05-15]   종료일: [2026-05-15]      │
│                                                   │
│ 색상: [color picker]   (선택, 미선택 시 타입별 기본) │
│                                                   │
│ 설명 (선택):                                      │
│ ┌─────────────────────────────────────────────┐ │
│ │                                             │ │
│ │                                             │ │
│ └─────────────────────────────────────────────┘ │
│                                                   │
│                              [취소] [저장]        │
└──────────────────────────────────────────────────┘
```

### 5.4 컴포넌트 트리

```
apps/web/src/app/admin/calendar/
└── page.tsx                              # 메인 페이지

apps/web/src/components/calendar/
├── MonthCalendar.tsx                     # 7×6 그리드 + 항목 렌더
├── EventList.tsx                         # 좌측 사이드 (당월 항목)
├── EntryModal.tsx                        # 추가·수정 폼
└── (CalendarTypeBadge.tsx)               # 타입별 색상·라벨 (옵션)

apps/web/src/components/home/
└── NextHolidaysCard.tsx                  # (옵션) 홈 카드
```

### 5.5 색상 매핑

```ts
const TYPE_COLORS: Record<CalendarType, string> = {
  PUBLIC_HOLIDAY: "#ef4444",  // red-500
  COMPANY_HOLIDAY: "#fb923c", // orange-400
  EVENT:           "#3b82f6", // blue-500
  WORKDAY:         "#9ca3af", // gray-400
};
```

`entry.color`가 있으면 우선 사용, 없으면 type 기본값.

### 5.6 사용자 플로우

```
[A] 관리자 등록 시
    관리 메뉴 ▾ → 📅 회사 달력 → /admin/calendar
    → [+ 항목 추가] → 모달 → 타입·제목·기간 → 저장
    → 캘린더 셀에 즉시 반영

[B] 일반 사용자 조회
    → 메뉴 미노출 (관리자 전용)
    → 홈 카드 "다음 휴일·행사"로 확인 (옵션)

[C] 다른 서비스 (2차 활성화 시)
    → attendance가 휴일 internal API 조회 → working day 제외
    → project가 Gantt 휴일 칠 처리
```

---

## 6. 권한 매트릭스

| 액션 | ADMIN | MANAGER | OPERATOR | VIEWER | 비인증 |
|---|:---:|:---:|:---:|:---:|:---:|
| 조회 (`GET /calendar*`) | ✅ | ✅ | ✅ | ✅ | ❌ |
| 등록 (`POST`) | ✅ | ❌ | ❌ | ❌ | ❌ |
| 수정 (`PATCH /:id`) | ✅ | ❌ | ❌ | ❌ | ❌ |
| 삭제 (`DELETE /:id`) | ✅ | ❌ | ❌ | ❌ | ❌ |
| internal API | (X-Internal-Token) | | | | |

미들웨어: `requireRole("ADMIN")` (`auth.middleware.ts` 기존)

---

## 7. 에러 처리

### 7.1 에러 코드

| HTTP | Code | 메시지 | 원인 |
|---|---|---|---|
| 400 | INVALID_INPUT | 입력값 오류 | Zod 위반 |
| 400 | INVALID_DATE_RANGE | 종료일은 시작일보다 빠를 수 없습니다 | endDate < startDate |
| 401 | UNAUTHENTICATED | 인증 필요 | 토큰 없음 |
| 403 | FORBIDDEN | 권한 없음 | ADMIN 아님 (변경 시) |
| 404 | CALENDAR_NOT_FOUND | 항목을 찾을 수 없습니다 | id 오류 |
| 500 | INTERNAL_ERROR | 서버 오류 | 예외 |

### 7.2 응답 포맷

기존 SSOT 따름:
```json
{ "error": { "code": "CALENDAR_NOT_FOUND", "message": "항목을 찾을 수 없습니다.", "details": { "id": "..." } } }
```

---

## 8. 보안

- [x] **인증**: 모든 엔드포인트 `authenticate` 미들웨어
- [x] **권한**: 변경 API `requireRole("ADMIN")`
- [x] **XSS**: description에 markdown 미사용 (plain text), HTML 입력 차단 (DTO 단계)
- [x] **SQL Injection**: Prisma ORM
- [x] **Internal Token**: `/internal/calendar/holidays`는 X-Internal-Token 검증
- [x] **Rate Limiting**: 1차 글로벌 (의도적, 별도 라우트 가드 없음)

---

## 9. 테스트 계획

### 9.1 테스트 범위

| 유형 | 대상 | 도구 |
|---|---|---|
| Integration | CRUD 6개 엔드포인트 | Supertest |
| Integration | 권한 매트릭스 (ADMIN/MANAGER/OPERATOR/VIEWER) | Supertest |
| Integration | INVALID_DATE_RANGE refinement | Supertest |
| Manual | 월간 캘린더 UI, 모달, 색상 | 브라우저 |

### 9.2 핵심 테스트 케이스

| FR | 케이스 | 기대 결과 |
|---|---|---|
| FR-04 | ADMIN 등록 → 201 | OK |
| FR-04 | MANAGER 등록 시도 → 403 | OK |
| FR-04 | endDate < startDate → 400 INVALID_DATE_RANGE | OK |
| FR-02 | from~to 기간 외 항목 제외 | OK |
| FR-02 | 단일일자 항목 from=to=date 조회 시 포함 | OK |
| FR-02 | 기간 항목 일부 겹쳐도 포함 (예: 5/20-25, from=5/22 to=5/23) | OK |
| FR-05 | ADMIN 수정 → 200 | OK |
| FR-06 | ADMIN 삭제 → 204, GET 시 404 | OK |
| FR-13 | OPERATOR가 변경 API 호출 → 403 | OK |
| FR-13 | OPERATOR가 GET 호출 → 200 | OK |

---

## 10. Clean Architecture

### 10.1 레이어 매핑

| 컴포넌트 | 레이어 | 위치 |
|---|---|---|
| `calendar.routes.ts`, `internal.routes.ts` (확장) | API | `services/auth/src/api/routes/` |
| `calendar.dto.ts` (Zod) | API | `services/auth/src/api/dtos/` |
| `calendar.service.ts` | Application | `services/auth/src/application/` |
| Prisma 모델 + 마이그레이션 | Infrastructure | `services/auth/prisma/` |
| Frontend 페이지 + 컴포넌트 | Presentation | `apps/web/src/app/admin/calendar/`, `apps/web/src/components/calendar/` |
| `calendarApi` | Infrastructure (FE) | `apps/web/src/lib/api.ts` |

---

## 11. 구현 가이드

### 11.1 파일 구조 (생성·수정)

```
services/auth/
├── prisma/
│   ├── schema.prisma                            # [수정] CompanyCalendarEntry 모델 + enum
│   └── migrations/YYYYMMDD_add_calendar/        # [생성]
└── src/
    ├── api/
    │   ├── routes/
    │   │   ├── calendar.routes.ts               # [생성]
    │   │   └── internal.routes.ts               # [수정] /calendar/holidays 추가
    │   └── dtos/
    │       └── calendar.dto.ts                  # [생성]
    ├── application/
    │   └── calendar.service.ts                  # [생성]
    └── index.ts                                 # [수정] route 등록

apps/web/
├── src/
│   ├── app/admin/calendar/
│   │   └── page.tsx                             # [생성]
│   ├── components/calendar/
│   │   ├── MonthCalendar.tsx                    # [생성]
│   │   ├── EventList.tsx                        # [생성]
│   │   └── EntryModal.tsx                       # [생성]
│   ├── components/AppLayout.tsx                 # [수정] 관리 메뉴 드롭다운에 "📅 회사 달력"
│   ├── lib/api.ts                               # [수정] calendarApi 추가
│   └── next.config.mjs                          # [수정] /api/v1/calendar/* rewrite
```

### 11.2 구현 순서 (2 단계)

**Do 1 — 백엔드 (auth-service)**
1. Prisma 스키마 추가 + 마이그레이션 (게시판과 동일 직접 SQL 패턴)
2. `calendar.dto.ts` (Zod 3개 스키마)
3. `calendar.service.ts` (CRUD + range)
4. `calendar.routes.ts` (6 endpoint)
5. `internal.routes.ts` 확장 (holidays 1개)
6. `index.ts` 라우트 등록

**Do 2 — 관리 UI**
1. `apps/web/src/lib/api.ts`에 `calendarApi` 추가
2. `apps/web/next.config.mjs` rewrites
3. `MonthCalendar`, `EventList`, `EntryModal` 컴포넌트
4. `/admin/calendar/page.tsx`
5. AppLayout 관리 메뉴 드롭다운에 "📅 회사 달력" 항목 추가
6. (옵션) `NextHolidaysCard` 홈 카드

### 11.3 의존성 설치

신규 패키지 없음.

---

## 12. 마이그레이션 / 롤백

### 12.1 마이그레이션

```sql
CREATE TYPE "CalendarEntryType" AS ENUM ('PUBLIC_HOLIDAY', 'COMPANY_HOLIDAY', 'EVENT', 'WORKDAY');

CREATE TABLE "company_calendar_entries" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "type" "CalendarEntryType" NOT NULL,
    "title" TEXT NOT NULL,
    "description" TEXT,
    "start_date" DATE NOT NULL,
    "end_date" DATE NOT NULL,
    "is_all_day" BOOLEAN NOT NULL DEFAULT true,
    "color" TEXT,
    "recurrence" TEXT,
    "target_department_id" TEXT,
    "start_time" TEXT,
    "end_time" TEXT,
    "created_by" TEXT NOT NULL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL
);

CREATE INDEX "company_calendar_entries_start_end_idx" ON "company_calendar_entries"("start_date", "end_date");
CREATE INDEX "company_calendar_entries_type_idx" ON "company_calendar_entries"("type");
```

### 12.2 롤백

```sql
DROP TABLE "company_calendar_entries";
DROP TYPE "CalendarEntryType";
```

기존 데이터에 영향 없음.

### 12.3 디스크 사용량 추정

- 항목 평균 200B × 연 ~50건 × 5년 = **50KB** (무시 가능)

---

## 13. 미해결 이슈 / 추후 결정

| # | 항목 | 상태 |
|---|---|---|
| 1 | UI: 다중일 항목 막대 렌더링 (시작 셀에서 종료 셀까지) | Design 명세 (1차 핵심) |
| 2 | 관리자 외 사용자가 캘린더 보고 싶을 때 — 별도 read-only 페이지? | **2차 결정** (1차는 ADMIN만 메뉴 노출) |
| 3 | 색상 자유 입력 vs 프리셋 | **프리셋 + 자유** 모두 허용 |
| 4 | iCalendar 내보내기 (.ics) | **2차 검토** |

---

## 14. 의사결정 로그

| # | 합의/결정 | 적용 |
|---|---|---|
| 1 | 1차 일회성만 | §3.1 recurrence nullable |
| 2 | 1차 수동 입력 | KASI API 미구현 |
| 3 | 1차 종일만 | §3.1 isAllDay default true, time 컬럼 더미 |
| 4 | 1차 조회·표시만 | §2.1 attendance/project 점선 표시 |
| 5 | 1차 전사만 | §3.1 targetDepartmentId nullable |
| 6 | 1차 알림 미포함 | §5.6 별도 알림 흐름 없음 |
| 7 | auth-service 확장 | §10 |
| 8 | 자체 캘린더 UI | §5.4 외부 라이브러리 0 |
| 9 | 색상 type별 기본 + 커스텀 | §5.5 |
| 10 | enum 4종 고정 | §3.1 |

---

## Version History

| Version | Date | Changes | Author |
|---|---|---|---|
| 1.0 | 2026-04-27 | 최초 Design 작성 (스키마·6 API·UI 와이어·권한·테스트 포함) | AI + Team |
| 1.1 | 2026-05-01 | `CalendarService` 응답에 `createdByName` 추가 — `attachCreatedByName()` private helper로 list/getById 일괄 매핑 (auth.users 직접 조회). UI에서 createdBy ID 노출 차단 (사용자-이름-표시 작업) | AI + Team |
